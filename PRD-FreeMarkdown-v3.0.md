# FreeMarkdown — macOS Markdown 编辑器产品需求说明书

| 字段 | 值 |
|---|---|
| 文档版本 | V3.0（架构重构与 MVP 收敛） |
| 前置版本 | V2.0（2026-09-03） |
| 创建日期 | 2026-09-03 |
| 产品名称 | FreeMarkdown |
| 目标平台 | macOS 13 Ventura+，Apple Silicon 优先 + Intel 兼容 |
| 授权模式 | 完全免费，开源（MIT） |
| 技术方案 | **Tauri 2 + React**（已确认） |
| 文档状态 | 发布，可进入开发 |

---

## 变更摘要（V2.0 → V3.0）

| 变更类型 | 内容 |
|---|---|
| **产品收敛** | 核心用户聚焦为「macOS 中文技术用户（程序员/产品经理/技术写作者）」；核心竞争力收敛为 5 件事：macOS 原生体验、Markdown 编辑体验、多编码兼容、中文/CJK 排版、免费开源 |
| **MVP 重构** | 版本规划从「V1.0 一步到位」重构为 V0.1 Alpha → V0.5 Beta → V1.0 → V1.5 → V2.0 渐进式路线 |
| **技术确认** | 确认采用 Tauri 2 + React 方案；明确编辑器引擎为 CodeMirror 6（源码模式）+ markdown-it 预览；WYSIWYG 延后至 V1.0 且明确技术路线 |
| **删除 DOCX** | 移除 DOCX 导出功能（V2.0 规划的 V2.0 DOCX 导出取消） |
| **快捷键修复** | 修复全部冲突：编辑模式用 `⌘1-⌘4`，Tab 切换用 `⌘⌥1-⌘⌥9`；全屏改为 `⌃⌘F`；主题切换改为 `⌘⌥T` |
| **新增模块** | M17 外部文件修改检测（P0）、M18 Undo/Redo 设计（P0）、M19 命令面板（P1）、M20 Markdown Lint（P1）、M21 文件 Diff（P1）、M22 文档模板系统（P1）、M23 大文件降级策略（P0） |
| **设计修正** | 自动保存简化为 2 种模式；版本历史改为智能快照策略；图片复制策略改为 4 选项；Markdown 扩展语法增加兼容性分层；编码检测增加置信度机制；主题系统精简 |
| **新增章节** | P0/P1/P2 功能优先级矩阵、技术架构决策、开发里程碑、风险清单 |

---

## 目录

- [1. 产品定位](#1-产品定位)
- [2. 产品信息架构](#2-产品信息架构)
- [3. 功能优先级矩阵](#3-功能优先级矩阵)
- [4. 技术架构决策](#4-技术架构决策)
- [5. 功能模块总览](#5-功能模块总览)
- [6. 详细需求](#6-详细需求)
  - [M01 界面与布局](#m01-界面与布局)
  - [M02 编辑模式](#m02-编辑模式)
  - [M03 多 Tab 与多窗口](#m03-多-tab-与多窗口)
  - [M04 主题体系](#m04-主题体系)
  - [M05 自定义主题](#m05-自定义主题)
  - [M06 Markdown 格式与兼容性分层](#m06-markdown-格式与兼容性分层)
  - [M07 扩展格式](#m07-扩展格式)
  - [M08 代码支持](#m08-代码支持)
  - [M09 图片功能](#m09-图片功能)
  - [M10 超链接与图标](#m10-超链接与图标)
  - [M11 文件管理与编码](#m11-文件管理与编码)
  - [M12 搜索功能](#m12-搜索功能)
  - [M13 导出与分享](#m13-导出与分享)
  - [M14 写作辅助](#m14-写作辅助)
  - [M15 设置与快捷键](#m15-设置与快捷键)
  - [M16 macOS 原生集成](#m16-macos-原生集成)
  - [M17 外部文件修改检测](#m17-外部文件修改检测)
  - [M18 Undo/Redo 设计](#m18-undoredo-设计)
  - [M19 命令面板](#m19-命令面板)
  - [M20 Markdown Lint](#m20-markdown-lint)
  - [M21 文件 Diff](#m21-文件-diff)
  - [M22 文档模板系统](#m22-文档模板系统)
  - [M23 大文件降级策略](#m23-大文件降级策略)
- [7. 版本规划与开发里程碑](#7-版本规划与开发里程碑)
- [8. 非功能需求](#8-非功能需求)
- [9. 竞品差异化分析](#9-竞品差异化分析)
- [10. 风险清单](#10-风险清单)
- [11. 验收清单](#11-验收清单)
- [12. 变更记录](#12-变更记录)

---

## 1. 产品定位

### 1.1 一句话定义

**面向 macOS 中文技术用户的现代 Markdown 编辑器——免费、原生、高性能、多编码、多 Tab。**

### 1.2 核心用户

| 层级 | 用户角色 | 核心诉求 |
|---|---|---|
| **核心用户** | 程序员 | 代码高亮、Mermaid 流程图、数学公式、快捷键、多 Tab |
| **核心用户** | 产品经理 | PRD 文档、流程图、表格、导出 PDF/HTML |
| **核心用户** | 技术写作者 | 长文排版、目录自动生成、导出 PDF、沉浸写作 |
| 次级用户 | 学生 / 研究人员 | LaTeX 公式、表格、PDF 导出 |
| 次级用户 | 博客作者 | 专注模式、字体主题、字数统计 |

### 1.3 核心竞争力（只抓 5 件事）

1. **macOS 原生体验**——Tauri 2 原生窗口、系统集成
2. **Markdown 编辑体验**——CodeMirror 6 + markdown-it + KaTeX + Mermaid + Shiki
3. **多编码兼容**——UTF-8/GBK/GB18030/Big5/Shift-JIS/EUC-KR + 自动识别 + 转换
4. **中文/CJK 排版**——CJK 字体 fallback、中英文混排优化
5. **免费开源**——MIT 许可证，核心功能永久免费

### 1.4 非目标

- 不做云端同步 / 在线协作
- 不做移动端
- 不做 DOCX 导出（已移除）
- 不做插件市场（V2.0 保留 API，不运营市场）
- V1.0 不试图同时超越 Typora / Obsidian / VS Code

---

## 2. 产品信息架构

```
FreeMarkdown (Tauri 2 + React)
│
├── 文件管理
│   ├── 最近文件
│   ├── 工作区 / 文件夹
│   ├── 多 Tab（拖拽 / 排序）
│   ├── 多窗口
│   ├── 外部文件修改检测
│   └── 文件恢复 / 版本历史
│
├── 编辑器
│   ├── 纯 Markdown 源码模式（CodeMirror 6）
│   ├── 实时双栏预览模式（markdown-it）
│   ├── 所见即所得模式（V1.0）
│   ├── 沉浸式写作模式
│   └── 分屏对比（V1.5）
│
├── Markdown 引擎
│   ├── Standard 层（CommonMark + GFM）
│   ├── Extended 层（KaTeX / Mermaid / TOC / Iconify）
│   └── 兼容性设置（Strict / GFM / FreeMarkdown Extended）
│
├── 主题系统
│   ├── 预置场景主题（3+）
│   ├── 代码高亮主题（独立）
│   ├── 自定义主题编辑器（V1.5）
│   └── 主题导入 / 导出（V1.5）
│
├── 编辑增强
│   ├── 搜索替换（文档内 + 全局）
│   ├── 命令面板（⌘⇧P）
│   ├── Markdown Lint
│   ├── 文件 Diff
│   └── 文档模板
│
├── 编码与国际化
│   ├── 多编码 + 自动识别（置信度）
│   ├── 编码转换
│   └── CJK 字体 fallback
│
├── 写作辅助
│   ├── 字数 / 字符 / 段落统计
│   ├── 番茄钟（V1.5）
│   └── 写作目标（V1.5）
│
├── 导出
│   ├── HTML
│   ├── PDF
│   └── 图片（PNG / JPEG）
│
└── 设置中心
    ├── 通用 / 编辑器 / 预览
    ├── 主题 / 代码 / 图片
    ├── Markdown 渲染 / 兼容性
    ├── 快捷键自定义
    └── 关于
```

---

## 3. 功能优先级矩阵

### P0（必须——V0.1 Alpha + V0.5 Beta）

| 编号 | 功能 | 版本 |
|---|---|---|
| M01 | 界面与布局（窗口/工具栏/状态栏/侧边栏） | V0.1 |
| M02 | 编辑模式（纯源码 + 双栏预览） | V0.1 |
| M03 | 多 Tab（新建/关闭/切换/拖拽排序） | V0.1 |
| M06 | CommonMark + GFM 标准格式 | V0.1 |
| M07 | Mermaid + KaTeX | V0.5 |
| M08 | 代码高亮（Shiki） | V0.1 |
| M11 | 文件管理（打开/保存/自动保存/最近文件） | V0.1 |
| M11 | 多编码（UTF-8/GBK/GB18030 + 自动识别） | V0.5 |
| M11 | CJK 字体 fallback | V0.5 |
| M12 | 文档内搜索替换 | V0.5 |
| M13 | 导出 PDF / HTML | V0.5 |
| M13 | 复制为 HTML / 纯文本 | V0.5 |
| M16 | macOS 原生集成（Dark/Light/Finder 拖拽） | V0.1 |
| M17 | 外部文件修改检测 | V0.5 |
| M18 | Undo/Redo 设计 | V0.1 |
| M23 | 大文件降级策略 | V0.5 |
| M04 | 3 个预置主题 + Dark/Light 跟随 | V0.1 |

### P1（重要——V1.0 + V1.5）

| 编号 | 功能 | 版本 |
|---|---|---|
| M02 | 所见即所得模式（WYSIWYG） | V1.0 |
| M02 | 沉浸式写作模式 | V1.0 |
| M03 | 多窗口支持 | V1.0 |
| M03 | Tab 分屏对比 | V1.5 |
| M04 | 完整主题体系（6 主题 + 代码高亮主题） | V1.0 |
| M05 | 自定义主题编辑器 | V1.5 |
| M05 | 主题导入/导出 | V1.5 |
| M07 | TOC / 脚注 / 任务列表 | V1.0 |
| M07 | Emoji / Iconify 图标 | V1.0 |
| M09 | 图片功能（拖拽/粘贴/管理） | V1.0 |
| M11 | 工作区 | V1.0 |
| M11 | 版本历史 | V1.5 |
| M12 | 全局搜索 | V1.0 |
| M13 | 导出图片 | V1.0 |
| M14 | 写作统计 + 番茄钟 + 写作目标 | V1.5 |
| M15 | 快捷键自定义 | V1.0 |
| M16 | Quick Look / iCloud Drive | V1.0 |
| M19 | 命令面板（⌘⇧P） | V1.0 |
| M20 | Markdown Lint | V1.0 |
| M21 | 文件 Diff | V1.5 |
| M22 | 文档模板系统 | V1.0 |

### P2（远期——V2.0+）

| 编号 | 功能 | 版本 |
|---|---|---|
| M06 | MDX 支持 | V2.0 |
| — | Git 集成 | V2.0 |
| — | 插件系统（扩展 API） | V2.0 |
| — | 主题市场（社区分享） | V2.0 |
| — | AI 写作辅助（Ollama / 本地 LLM） | V3.0 |

---

## 4. 技术架构决策

### 4.1 方案确认：Tauri 2 + React

经评审对比，确认采用 **Tauri 2 + React** 方案。

#### 方案对比

| 维度 | 方案 A: Tauri 2 + React | 方案 B: Swift + WKWebView Hybrid | 方案 C: Electron + React |
|---|---|---|---|
| **macOS 原生体验** | ★★★★☆ 原生窗口，系统集成良好 | ★★★★★ 完全原生 | ★★★☆☆ 窗口略显粗糙 |
| **性能** | ★★★★★ Rust 后端，低内存 | ★★★★★ 原生性能 | ★★☆☆☆ 内存高 |
| **包体积** | ★★★★★ ~10MB | ★★★★★ ~15MB | ★★☆☆☆ ~150MB+ |
| **开发速度** | ★★★★☆ React 生态成熟 | ★★★☆☆ Swift 学习曲线 | ★★★★★ 生态最成熟 |
| **编辑器生态** | ★★★★★ CodeMirror/markdown-it 原生 Web | ★★★★☆ 通过 WebView 使用 | ★★★★★ 同 Tauri |
| **跨平台潜力** | ★★★★★ 未来支持 Win/Linux | ★☆☆☆☆ 仅 macOS | ★★★★★ 跨平台 |
| **维护成本** | ★★★★☆ 中等 | ★★★☆☆ Swift + Web 双栈 | ★★★★☆ 中等 |

**决策理由：**
1. CodeMirror 6 + markdown-it + Mermaid + KaTeX + Shiki 生态在 Web 端最成熟。
2. React 前端开发速度快，拥有完整技能链支持。
3. Rust 后端性能优秀，内存占用远低于 Electron。
4. 包体积小（~10MB），符合轻量编辑器定位。
5. 未来可扩展到 Windows/Linux。
6. 当前环境 Node.js/pnpm 已就绪，仅需安装 Rust。

### 4.2 架构分层

```
┌───────────────────────────────────────────────┐
│                 UI 层 (React 19)               │
│  编辑区 │ 预览区 │ 工具栏 │ 侧边栏 │ Tab栏     │
│  设置面板 │ 命令面板 │ 状态栏                   │
├───────────────────────────────────────────────┤
│             编辑器引擎 (CodeMirror 6)          │
│  语法高亮 │ 快捷键 │ 自动补全 │ 折叠 │ 搜索    │
│  Undo/Redo 栈（每 Tab 独立，1000 步）          │
├───────────────────────────────────────────────┤
│         Markdown 渲染管线 (markdown-it)        │
│  解析 → AST → 插件链 → HTML → 增强             │
│  兼容性分层: Standard / Extended               │
├───────────────────────────────────────────────┤
│              渲染引擎                           │
│  KaTeX (公式) │ Mermaid (图表)                 │
│  Shiki (代码高亮) │ Iconify (图标)             │
├───────────────────────────────────────────────┤
│              主题引擎 (CSS Variables)          │
│  预置主题 │ 自定义主题 │ 代码高亮主题           │
├───────────────────────────────────────────────┤
│           原生能力层 (Rust / Tauri 2)          │
│  文件IO │ 编码检测 │ 文件监听 │ 窗口管理       │
│  剪贴板 │ PDF导出 │ SQLite │ 系统菜单          │
└───────────────────────────────────────────────┘
```

### 4.3 核心技术选型

| 功能 | 技术 | 说明 |
|---|---|---|
| 应用框架 | Tauri 2.0 | Rust 后端 + Web 前端 |
| 前端框架 | React 19 + TypeScript | 组件化开发 |
| 状态管理 | Zustand | 轻量全局状态 |
| 编辑器内核 | CodeMirror 6 | 源码编辑，插件生态丰富 |
| Markdown 解析 | markdown-it + 扩展插件链 | CommonMark/GFM |
| 代码高亮 | Shiki | VS Code 级别高亮 |
| 数学公式 | KaTeX | 快速 LaTeX 渲染 |
| 图表渲染 | Mermaid.js | 流程图/时序图等 |
| 图标 | Iconify | 200,000+ 图标 |
| PDF 导出 | Rust print / WebView print | — |
| 数据库 | SQLite (可选) | 文档元数据 |
| 编码检测 | chardetng (Rust) | 多编码自动识别 |
| 文件监听 | Tauri fs watch | 外部修改检测 |
| 构建工具 | Vite | 快速 HMR 开发 |

### 4.4 WYSIWYG 技术路线（V1.0）

**决策：采用 CodeMirror 6 decorations 方案，不引入 ProseMirror。**

| 方案 | 说明 | 采纳 |
|---|---|---|
| CodeMirror 6 decorations | 在源码上叠加样式装饰，输入 `# ` 后即时渲染标题样式，底层仍为 Markdown 源码 | ✅ 采纳 |
| ProseMirror | 完全的富文本编辑器，需 Markdown ↔ 富文本双向转换 | ❌ 不采纳 |

**理由：**
- CodeMirror decorations 方案保留 Markdown 源码语义，不存在双向转换难题。
- 光标和撤销栈天然正确（始终操作源码）。
- Typora 类似方案，用户感知为 WYSIWYG 但底层无语义丢失。
- 不引入第二个编辑器引擎，降低架构复杂度。
- 风险可控：decoration 渲染失败时退回到纯源码显示。

---

## 5. 功能模块总览

| 编号 | 功能模块 | 功能点数 | 优先级 | 首发版本 |
|---|---|---|---|---|
| M01 | 界面与布局 | 10 | P0 | V0.1 |
| M02 | 编辑模式 | 4 | P0 | V0.1 |
| M03 | 多 Tab 与多窗口 | 8 | P0 | V0.1 |
| M04 | 主题体系 | 7 | P0 | V0.1 |
| M05 | 自定义主题 | 8 | P1 | V1.5 |
| M06 | Markdown 格式与兼容性分层 | 3 | P0 | V0.1 |
| M07 | 扩展格式 | 10 | P0 | V0.5 |
| M08 | 代码支持 | 5 | P0 | V0.1 |
| M09 | 图片功能 | 4 | P1 | V1.0 |
| M10 | 超链接与图标 | 3 | P0 | V1.0 |
| M11 | 文件管理与编码 | 10 | P0 | V0.1 |
| M12 | 搜索功能 | 2 | P0 | V0.5 |
| M13 | 导出与分享 | 6 | P0 | V0.5 |
| M14 | 写作辅助 | 4 | P1 | V1.5 |
| M15 | 设置与快捷键 | 5 | P0 | V0.1 |
| M16 | macOS 原生集成 | 4 | P0 | V0.1 |
| M17 | 外部文件修改检测 | 1 | P0 | V0.5 |
| M18 | Undo/Redo 设计 | 1 | P0 | V0.1 |
| M19 | 命令面板 | 1 | P1 | V1.0 |
| M20 | Markdown Lint | 1 | P1 | V1.0 |
| M21 | 文件 Diff | 1 | P1 | V1.5 |
| M22 | 文档模板系统 | 1 | P1 | V1.0 |
| M23 | 大文件降级策略 | 1 | P0 | V0.5 |

---

## 6. 详细需求

### M01 界面与布局

#### M01-01 窗口与标题栏

| 属性 | 说明 |
|---|---|
| 操作/方法 | Tauri 2 原生窗口，支持 macOS 红绿灯按钮 |
| 规则 | 标题栏显示文件名 + 修改标记（`●`）；未保存显示 `未命名` |
| 快捷键 | `⌘W` 关闭 Tab/窗口；`⌘M` 最小化 |
| 验收 | 窗口拖拽正常；全屏后退出恢复原大小；多窗口独立 |

#### M01-02 布局模式

| 模式 | 说明 | 快捷键 |
|---|---|---|
| 仅编辑 | 全屏源码编辑 | `⌘1` |
| 仅预览 | 全屏渲染预览 | `⌘2` |
| 分屏对比 | 左编辑右预览，实时同步滚动 | `⌘3`（默认） |

- 分屏分隔条可拖动调整宽度比例（3:7 ~ 7:3），位置持久化。
- 双向滚动同步。

#### M01-03 侧边栏（文件树 / 工作区）

| 属性 | 说明 |
|---|---|
| 功能 | 展示已打开文件夹/工作区的文件树 |
| 操作 | 点击打开；右键：新建、重命名、删除、Finder 中显示、复制路径 |
| 切换 | `⌘\` |
| 搜索 | 顶部搜索框实时过滤 |
| 验收 | 500+ 文件加载 < 1s |

#### M01-04 工具栏

| 属性 | 说明 |
|---|---|
| 位置 | 标题栏下方 |
| 内容 | 布局切换、主题切换、快速格式按钮、导出、设置 |
| 可定制 | 可隐藏/显示，设置中配置按钮可见性 |

#### M01-05 状态栏

```
Words: 1,234  Characters: 6,720  Lines: 120  Reading: 5 min  Markdown | UTF-8 | Saved
```

- 实时更新；数字千分位格式化；可自定义显示项。

#### M01-06 面包屑导航

- 编辑区顶部显示当前光标所在标题层级面包屑；点击跳转。

#### M01-07 字数统计与阅读时间

- 中文字数 + 英文词数 + 阅读时间（300 字/分钟）；中英文混合正确统计。

#### M01-08 拖拽支持

| 类型 | 行为 |
|---|---|
| 拖拽文件 | `.md` 文件到窗口新 Tab 打开 |
| 拖拽图片 | 自动插入图片语法，复制策略见 M09 |
| 拖拽文本 | 以引用方式插入 |

#### M01-09 大纲导航面板

- 预览区右侧可固定显示大纲面板；点击标题跳转；实时更新。

#### M01-10 全屏

- `⌃⌘F` 进入/退出全屏；全屏下隐藏侧边栏和工具栏。

---

### M02 编辑模式

#### M02-01 纯 Markdown 源码模式（V0.1）

| 属性 | 说明 |
|---|---|
| 描述 | 类似 VS Code，用户直接编辑 Markdown 源码 |
| 引擎 | CodeMirror 6 |
| 特性 | 语法高亮、行号、代码折叠、自动补全 |
| 快捷键 | `⌘1` |
| 验收 | 语法着色正确；10,000 行流畅滚动 |

#### M02-02 实时双栏预览模式（V0.1）

| 属性 | 说明 |
|---|---|
| 描述 | 左编辑右预览，输入后 < 100ms 预览更新 |
| 同步 | 双向滚动同步；点击预览跳转编辑位置 |
| 宽度 | 分隔条可调（3:7 ~ 7:3） |
| 增量渲染 | 仅重新渲染变化部分 |
| 快捷键 | `⌘3`（默认） |
| 验收 | 无闪烁；大文档增量渲染不卡顿 |

#### M02-03 所见即所得模式（V1.0）

| 属性 | 说明 |
|---|---|
| 描述 | 输入 Markdown 语法后即时渲染为样式化文本，底层仍为 Markdown 源码 |
| 技术 | CodeMirror 6 decorations（不引入 ProseMirror） |
| 行为 | 退格/输入时光标在渲染后的文本中操作；切换回源码模式可看到原始 Markdown |
| 快捷键 | `⌘4` |
| 验收 | 渲染后切回源码保留原始语法；光标定位准确；撤销栈正确 |
| 风险 | decoration 渲染失败时退回纯源码显示（降级安全） |

#### M02-04 沉浸式写作模式（V1.0）

| 属性 | 说明 |
|---|---|
| 描述 | 全屏、隐藏侧边栏和工具栏，自动弱化非当前段落 |
| 弱化 | 非当前段落透明度降至 30% |
| 内容宽度 | 可设置最大宽度（600/800px），居中 |
| 退出 | `Esc` |

---

### M03 多 Tab 与多窗口

#### M03-01 多 Tab 支持（V0.1）

| 属性 | 说明 |
|---|---|
| 功能 | 单窗口内多 Tab 打开多个文件 |
| 限制 | 单窗口最多 20 个 Tab，超出可横向滚动 |
| 验收 | 切换 < 50ms；恢复光标位置和滚动位置 |

#### M03-02 Tab 新建与关闭（V0.1）

| 操作 | 快捷键 | 说明 |
|---|---|---|
| 新建 Tab | `⌘N` | 空白文档 |
| 关闭当前 | `⌘W` | 有未保存修改时弹出确认 |
| 重新打开 | `⇧⌘T` | 恢复最近关闭的 Tab（最多 10 个） |

Tab 右键菜单：

```
关闭          ⌘W
关闭其他
关闭右侧
关闭全部
────────────
固定
复制文件路径
在 Finder 中显示
────────────
重新打开       ⇧⌘T
```

#### M03-03 Tab 拖拽排序（V0.1）

- 拖拽时半透明跟随鼠标；目标位置显示插入指示线；顺序持久化。

#### M03-04 Tab 分屏对比（V1.5）

| 属性 | 说明 |
|---|---|
| 功能 | 拖拽 Tab 到编辑区边缘创建分屏 |
| 方式 | 垂直分屏（左|右）、水平分屏（上—下） |
| 用途 | 对比两个文档、编辑同时参考 |

#### M03-05 Tab 快捷键（V0.1）

**已修复冲突：编辑模式用 `⌘1-⌘4`，Tab 切换用 `⌘⌥1-⌘⌥9`。**

| 功能 | 快捷键 |
|---|---|
| 新建 Tab | `⌘N` |
| 关闭当前 Tab | `⌘W` |
| 重新打开关闭 Tab | `⇧⌘T` |
| 下一个 Tab | `⌘⌥→` |
| 上一个 Tab | `⌘⌥←` |
| 切换到第 1-9 个 Tab | `⌘⌥1` ~ `⌘⌥9` |

兼容：`⌘⇧[` / `⌘⇧]` 切换 Tab。

#### M03-06 多窗口支持（V1.0）

| 属性 | 说明 |
|---|---|
| 功能 | 多个独立 macOS 窗口，各有自己的 Tab 组 |
| 操作 | 菜单「窗口 → 新建窗口」/ `⌘⇧N` |
| 独立 | 每个窗口独立记忆布局、主题、侧边栏状态 |

#### M03-07 固定 Tab（V1.0）

- 固定的 Tab 缩小为图标；不被「关闭其他」影响。

#### M03-08 文件管理架构

```
Application
├── Window 1
│   ├── Tab 1 → README.md
│   ├── Tab 2 → PRD.md
│   └── Tab 3 → API.md
├── Window 2
│   ├── Tab 1 → Notes.md
│   └── Tab 2 → Ideas.md
└── Workspace
    ├── Project A
    └── Project B
```

---

### M04 主题体系

#### M04-01 预置主题

**V0.1 提供 3 个，V1.0 补充至 6 个。**

| 编号 | 主题名称 | 首发 | 适用场景 | 特点 |
|---|---|---|---|---|
| T01 | **办公白**（Office Light） | V0.1 | 日常办公 | 白底黑字，苹方，16px，行高 1.8 |
| T02 | **夜间深色**（Dark Night） | V0.1 | 夜间 | 深灰底浅字，低蓝光 |
| T03 | **程序员阅读**（Code Reader） | V0.1 | 技术文档 | 深色背景，JetBrains Mono，One Dark 代码高亮 |
| T04 | **沉浸写作**（Focus Writer） | V1.0 | 长文创作 | 米白底，霞鹜文楷，高行距，内容宽度限制 |
| T05 | **极简灰**（Minimal Gray） | V1.0 | 极简风格 | 大量留白，隐藏边框 |
| T06 | **学术论文**（Academic） | V1.0 | 论文 | Times New Roman/宋体，公式优化，段落缩进 |

#### M04-02 代码高亮主题（独立）

| 类型 | 主题名 |
|---|---|
| 浅色 | GitHub Light、VS Code Light |
| 深色 | GitHub Dark、Dracula、Monokai、One Dark、Nord |

- 代码高亮主题与编辑器主题可独立组合。
- 默认跟随编辑器主题（浅色→GitHub Light，深色→One Dark）。

#### M04-03 主题切换

| 属性 | 说明 |
|---|---|
| 操作 | 工具栏下拉 / 设置面板 / `⌘⌥T` |
| 预览 | 实时预览 |
| 动画 | 200ms 过渡 |

#### M04-04 跟随系统外观

| 属性 | 说明 |
|---|---|
| 功能 | 跟随 macOS 深色/浅色模式 |
| 规则 | 分别配置浅色主题和深色主题 |
| 验收 | 系统切换时 300ms 内切换 |

#### M04-05 主题应用范围

| 范围 | 说明 |
|---|---|
| 编辑区 | 语法高亮、背景、字体 |
| 预览区 | 渲染 HTML 样式 |
| UI 界面 | 工具栏、侧边栏、状态栏 |
| 代码高亮 | 独立配置 |

#### M04-06 主题管理（V1.5）

| 属性 | 说明 |
|---|---|
| 新建 | 从现有主题「另存为」 |
| 删除 | 可删除自定义主题；预置不可删 |
| 导入/导出 | `.fmtheme` (JSON) |
| 恢复默认 | 一键重置 |

#### M04-07 主题预览卡片

- 主题选择面板中每个主题展示缩略卡片（标题/正文/代码/表格色样）。

---

### M05 自定义主题（V1.5）

#### M05-01 主题编辑器布局

```
┌────────────────────────────────────────┐
│ 主题编辑器                              │
├───────────────┬────────────────────────┤
│  设置面板      │     实时预览            │
│  字体/颜色     │  (编辑 + 预览分屏)      │
│  页面/标题     │                        │
│  表格/图片     │                        │
│  代码          │                        │
└───────────────┴────────────────────────┘
```

修改后预览 < 100ms 更新。

#### M05-02 字体设置

| 参数 | 范围 | 默认 |
|---|---|---|
| 正文字体 | 系统字体 + 推荐 | SF Pro Text / 苹方 |
| 标题字体 | 同上 | SF Pro Display / 苹方 |
| 代码字体 | 等宽字体 | JetBrains Mono |
| 字体大小 | 10–24px | 16px |
| 代码字体大小 | 10–20px | 14px |
| 行高 | 1.2–2.5 | 1.7 |
| 字间距 | -2px ~ 5px | 0px |
| 段间距 | 0.5–3em | 1em |
| 字重 | 300–700 | 400 |

推荐字体：苹方、思源黑体、思源宋体、霞鹜文楷、JetBrains Mono、Menlo、SF Mono。

#### M05-03 颜色设置

| 参数 | 说明 |
|---|---|
| 背景色 | 编辑区/预览区/代码块/引用块/表格交替行 |
| 正文颜色 | 普通文本 |
| 标题颜色 | H1–H6 可分别设置 |
| 链接颜色 | 普通/已访问/hover |
| 代码颜色 | 行内/代码块 |
| 表格颜色 | 表头背景/边框/交替行 |

操作：macOS ColorPicker + HEX/RGB/HSL 手动输入。

#### M05-04 背景与页面设置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 背景类型 | 纯色 / 渐变 / 图片 | 纯色 |
| 内容最大宽度 | 400–1200px | 800px |
| 左右边距 | 20–100px | 40px |

> **精简说明：** V2.0 中的背景模糊、渐变方向等多参数保留但降为 P2，V1.5 仅支持纯色背景 + 最大宽度 + 边距。

#### M05-05 标题配置

每级标题（H1–H6）支持：字号、颜色、字重、间距、下划线、分割线（H1 默认开）。

#### M05-06 代码块配置

背景颜色、字体、字体大小、圆角、边框、代码高亮主题、行号、内边距。

#### M05-07 表格配置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 风格 | 简约线框/斑马纹/边框网格/无边框 | 斑马纹 |
| 表头背景 | 颜色选择器 | 浅灰 |
| 边框颜色 | 颜色选择器 | 浅灰 |
| 斑马纹 | 开/关 | 开 |
| 单元格内边距 | 4–20px | 8px |
| 圆角 | 0–12px | 4px |

#### M05-08 图片配置

最大宽度（50%–100%）、默认居中、圆角（0–20px）、阴影（无/浅/中/深）、点击放大、间距。

#### M05-09 主题文件格式

```json
{
  "name": "My Writing Theme",
  "version": "1.0",
  "font": {
    "body": "LXGW WenKai",
    "code": "JetBrains Mono",
    "size": 18,
    "lineHeight": 1.8,
    "weight": 400
  },
  "colors": {
    "background": "#F7F3E8",
    "text": "#333333",
    "heading": "#222222",
    "link": "#0066cc",
    "code": "#c7254e"
  },
  "page": { "maxWidth": "800px", "padding": "40px" },
  "table": { "headerBg": "#f5f5f5", "zebra": true },
  "image": { "maxWidth": "80%", "borderRadius": "8px", "shadow": true }
}
```

---

### M06 Markdown 格式与兼容性分层

#### M06-01 兼容性分层（新增）

**这是 V3.0 的重要新增。** 为避免扩展语法导致文件在其他编辑器中不兼容，增加兼容性分层设置。

```
Markdown Compatibility
○ Strict CommonMark     — 仅标准 CommonMark
○ GitHub Flavored       — CommonMark + GFM（表格/任务列表/删除线/自动链接）
● FreeMarkdown Extended   — GFM + KaTeX + Mermaid + TOC + Iconify + 图片尺寸
```

| 设置项 | 说明 |
|---|---|
| Strict CommonMark | 最严格兼容；不渲染任何扩展语法 |
| GitHub Flavored | GFM 兼容；支持表格/任务列表/删除线/自动链接，但不渲染 KaTeX/Mermaid/TOC 等 |
| FreeMarkdown Extended（默认） | 全部扩展语法启用 |

- 用户可在设置中切换兼容性级别。
- 切换后预览区重新渲染。
- 导出时在文件头部可选写入兼容性声明注释（`<!-- FreeMarkdown Extended -->`）。

#### M06-02 标准格式支持（CommonMark + GFM）

| 编号 | 格式元素 | 语法 | 优先级 | 兼容层 |
|---|---|---|---|---|
| F01 | 标题 | `# H1` ~ `###### H6` | P0 | CommonMark |
| F02 | 段落 | 空行分隔 | P0 | CommonMark |
| F03 | 加粗 | `**bold**` | P0 | CommonMark |
| F04 | 斜体 | `*italic*` | P0 | CommonMark |
| F05 | 删除线 | `~~del~~` | P0 | GFM |
| F06 | 行内代码 | `` `code` `` | P0 | CommonMark |
| F07 | 代码块 | ` ```lang ... ``` ` | P0 | CommonMark |
| F08 | 有序列表 | `1. item` | P0 | CommonMark |
| F09 | 无序列表 | `- item` | P0 | CommonMark |
| F10 | 任务列表 | `- [x] done` | P0 | GFM |
| F11 | 引用块 | `> quote` | P0 | CommonMark |
| F12 | 水平分割线 | `---` | P0 | CommonMark |
| F13 | 链接 | `[text](url)` | P0 | CommonMark |
| F14 | 图片 | `![alt](url)` | P0 | CommonMark |

#### M06-03 标准格式渲染规格

- **标题**：H1 下方自动分割线（可关闭）；自动生成锚点。
- **嵌套列表**：Tab/Shift+Tab 调整缩进。
- **任务列表**：预览区可点击切换勾选，同步更新源码。
- **链接**：行内/引用式/自动链接；内部 `.md` 链接在编辑器内打开。
- **图片**：本地/网络/拖拽/粘贴；支持尺寸扩展语法 `![alt](url =200x100)`（Extended 层）。

---

### M07 扩展格式

| 编号 | 功能 | 语法 | 优先级 | 首发版本 | 兼容层 |
|---|---|---|---|---|---|
| E01 | 数学公式（行内） | `$E=mc^2$` | P0 | V0.5 | Extended |
| E02 | 数学公式（块级） | `$$...$$` | P0 | V0.5 | Extended |
| E03 | Mermaid 图表 | ` ```mermaid ... ``` ` | P0 | V0.5 | Extended |
| E04 | 目录（TOC） | `[TOC]` | P0 | V1.0 | Extended |
| E05 | 脚注 | `[^1]` | P0 | V1.0 | Extended |
| E06 | 定义列表 | `Term\n: Definition` | P1 | V1.5 | Extended |
| E07 | 表格扩展 | GFM 表格 + 列对齐 | P0 | V0.1 | GFM |
| E08 | Emoji | `:smile:` | P1 | V1.0 | Extended |
| E09 | Iconify 图标 | `:icon[mdi-home]:` | P1 | V1.0 | Extended |
| E10 | HTML 安全子集 | `<sub>` `<sup>` `<kbd>` 等 | P0 | V0.1 | Extended |

#### 数学公式
- 引擎：KaTeX（优先）。
- 支持 LaTeX 全语法；`\[...\]` 和 `\(...\)` 别名。
- 渲染失败显示红色错误 + 原始源码。

#### Mermaid 图表
- 支持：flowchart、sequenceDiagram、classDiagram、stateDiagram、erDiagram、gantt、pie、mindmap、timeline、journey。
- 主题跟随；点击放大；渲染失败显示错误。

#### TOC
- `[TOC]` 自动生成标题目录；可点击跳转；实时更新；可设最大层级。
- 与大纲面板同步但独立于文档内容。

#### 脚注
- `[^id]` 上标链接 + 底部注释；返回箭头；自动编号。

#### HTML 安全子集
- 允许：`<sub>` `<sup>` `<kbd>` `<u>` `<mark>` `<details>` `<summary>` `<abbr>`。
- 禁用：`<script>` `<iframe>` `<object>` `<embed>` `<form>` `<input>` `<style>`。

---

### M08 代码支持

#### M08-01 代码块语法高亮（V0.1）

| 属性 | 说明 |
|---|---|
| 引擎 | Shiki |
| 语言 | 200+（Java, Python, JS, TS, Go, Rust, C, C++, Shell, SQL, JSON, YAML, Dockerfile, Swift, Kotlin, PHP, Ruby 等） |
| 语言标签 | 代码块右上角显示 |

#### M08-02 代码块增强（V0.5）

| 功能 | 说明 |
|---|---|
| 复制按钮 | 右上角一键复制 |
| 行号 | 可开关 |
| 自动换行 | 可开关 |
| 代码折叠 | 超过阈值（默认 20 行）自动折叠 |
| 高亮指定行 | ` ```js {1,3-5} ` 语法 |
| 语言补全 | 输入 `` ``` `` 后弹出语言列表 |

#### M08-03 行内代码（V0.1）

- 不同背景色 + 等宽字体 + 圆角；`⌘E` 包裹。

#### M08-04 代码高亮主题（V0.1）

见 M04-02，独立于编辑器主题。

#### M08-05 代码块配置

见 M05-06。

---

### M09 图片功能（V1.0）

#### M09-01 图片插入

| 方式 | 说明 |
|---|---|
| 本地拖拽 | 插入图片语法，复制策略见下 |
| 文件选择 | 菜单「编辑 → 插入图片」 |
| 粘贴图片 | 截图后 `⌘V`，自动保存 PNG |
| URL 图片 | 手动输入 |
| 路径补全 | 输入 `!` 后补全本地路径 |

#### M09-02 图片复制策略（重新设计）

**V2.0 问题：自动复制到 `assets/` 可能污染用户目录。V3.0 修正为 4 种策略可选。**

| 策略 | 说明 | 默认 |
|---|---|---|
| 保持原路径 | 不复制，直接引用原路径 | — |
| 复制到文档 assets | 复制到文档同目录 `assets/images/` | ✅ 工作区模式 |
| 复制到工作区 assets | 复制到工作区根目录 `assets/images/` | ✅ 工作区模式 |
| 每次询问 | 每次拖拽/粘贴时弹出选择对话框 | ✅ 非工作区模式 |

- 设置中可配置默认策略。
- 非工作区模式下首次拖拽/粘贴图片时弹窗询问，用户可选择并「记住选择」。

#### M09-03 图片管理器（V1.0）

| 属性 | 说明 |
|---|---|
| 功能 | 网格缩略图展示当前文档/工作区图片 |
| 操作 | 点击插入；右键：删除、重命名、Finder 中显示 |
| 删除检查 | 删除图片时检查文档中是否有引用并提示 |

#### M09-04 图片编辑（V1.0）

| 功能 | 语法/方式 |
|---|---|
| 调整大小 | `![alt](url =200x100)` 或 `{width=80%}` |
| 对齐 | 左/居中/右 |
| 标题 | `![alt](url "caption")` 渲染为 caption |
| 点击放大 | 预览区点击全屏查看 |

---

### M10 超链接与图标（V1.0）

#### M10-01 超链接

| 功能 | 说明 |
|---|---|
| 行内链接 | `[text](url)` |
| 引用式链接 | `[text][ref]` + `[ref]: url` |
| 自动链接 | 裸 URL 自动转为链接 |
| URL Tooltip | hover 显示 URL |
| 外部链接 | 系统默认浏览器打开 |
| 内部链接 | `.md` 文件在编辑器内打开（新 Tab） |
| 锚点链接 | `[章节](#heading)` 跳转 |
| 本地文件链接 | 点击本地文件路径在编辑器中打开 |

#### M10-02 Emoji（V1.0）

- `:shortcode:` 语法；内置 Emoji 速查面板；支持原生 Emoji。

#### M10-03 Iconify 图标（V1.0）

- `:icon[icon-set-name]:` 语法（如 `:icon[mdi-home]:`）；200,000+ 图标；跟随文字颜色。

---

### M11 文件管理与编码

#### M11-01 新建文件（V0.1）

- `⌘N` / 菜单 / 工具栏；空白文档；首次保存弹出对话框。
- V1.0 支持模板选择（见 M22）。

#### M11-02 打开文件（V0.1）

- `⌘O` / 拖拽；`.md` `.markdown` `.mdx` `.txt`；1MB+ < 2s。

#### M11-03 保存与自动保存（V0.1，重新设计）

**V2.0 问题：三种自动保存策略过于复杂。V3.0 简化为两种模式。**

| 模式 | 说明 |
|---|---|
| 手动保存（默认） | 仅 `⌘S` 时保存到磁盘 |
| 自动保存 | 停止输入 5s 后保存到磁盘 + 窗口失焦时保存 |

**草稿机制（独立于文件保存）：**

```
编辑器内存
  ↓ 每 30s
Application Support/Drafts/（临时草稿）
  ↓ 用户 ⌘S
实际文件
```

- 草稿始终保存到临时目录（无论是否开启自动保存）。
- 正常退出时清理草稿；异常退出后恢复。
- 草稿保存不触发 Git 文件变化（写临时目录而非用户文件）。

#### M11-04 未保存文件管理（V0.1）

关闭有未保存修改的 Tab 时：

```
┌──────────────────────────────────┐
│  此文件尚未保存                  │
│  PRD.md 有未保存的修改。          │
│  [取消]  [不保存]  [保存]        │
└──────────────────────────────────┘
```

#### M11-05 文件恢复（V0.5）

异常退出后重开：

```
┌──────────────────────────────────┐
│  发现未恢复的文件                 │
│  PRD.md                          │
│  最后修改：2026-09-03 09:32      │
│  [恢复]  [丢弃]                  │
└──────────────────────────────────┘
```

#### M11-06 版本历史（V1.5，重新设计）

**V2.0 问题：每次保存完整快照浪费空间。V3.0 改为智能快照策略。**

| 触发条件 | 说明 |
|---|---|
| 手动保存 | 每次 `⌘S` 创建快照 |
| 定时 | 每 5 分钟（如有修改） |
| 应用关闭 | 退出时创建快照 |
| 重要版本 | 用户手动标记「重要版本」 |

| 属性 | 说明 |
|---|---|
| 展示 | 侧边面板时间线（时间 + 变更摘要） |
| 操作 | 预览某版本；恢复到指定版本 |
| 限制 | 最多 100 个快照；超出删最旧 |
| 存储 | V1.5 存储完整快照；V2.0 规划 diff-based 增量存储 |
| 位置 | `~/Library/Application Support/FreeMarkdown/versions/` |

#### M11-07 多编码支持（V0.5）

| 属性 | 说明 |
|---|---|
| 支持编码 | UTF-8、UTF-8 BOM、GBK、GB2312、GB18030、UTF-16 LE/BE、ASCII、Big5、Shift-JIS、EUC-KR |
| 自动识别 | 打开文件时自动检测编码 |
| 编码转换 | 另存为不同编码 |
| 状态栏 | 显示当前编码 |
| 操作 | 菜单「文件 → 编码 → 重新以编码打开」 |

#### M11-08 编码检测置信度机制（新增）

**V2.0 问题：某些短文本无法准确识别编码。V3.0 增加置信度机制。**

```
自动检测
  ↓
置信度排序
  ↓
≥ 90% → 自动使用
< 90% → 弹窗让用户选择
  ↓
用户可手动切换
  ↓
重新加载文件
```

检测结果显示：

```
编码检测
  ● UTF-8        置信度 98%
  ○ GB18030      置信度 72%
  ○ Shift-JIS    置信度 31%
```

- 置信度 ≥ 90%：自动使用该编码。
- 置信度 < 90%：弹窗展示候选编码及置信度，用户选择。
- 用户选择后可「记住」该文件的编码偏好。

#### M11-09 CJK 字体 fallback（V0.5）

| 属性 | 说明 |
|---|---|
| 功能 | 主字体不包含某字符时自动 fallback |
| fallback 链 | 主字体 → 苹方 (PingFang SC) → 思源黑体 → 系统默认 |
| 配置 | 设置中可自定义 CJK fallback 字体 |
| 验收 | 中英日韩混排正确显示，无方块 |

#### M11-10 最近文件与工作区

| 属性 | 说明 |
|---|---|
| 最近文件 | 菜单显示最近 20 个文件 |
| 首页 | 无文件时显示最近文件列表 |
| 工作区 | 打开文件夹作为工作区，侧边栏显示文件树 |
| 文件树操作 | 新建/重命名/删除/Finder 显示/移动 |

---

### M12 搜索功能

#### M12-01 文档内搜索与替换（V0.5）

| 属性 | 说明 |
|---|---|
| 唤起 | `⌘F` 搜索；`⌘⌥F` 替换 |
| 功能 | 区分大小写、正则、全词匹配 |
| 统计 | 匹配数量和当前位置 |
| 验收 | 大文件搜索 < 200ms |

#### M12-02 全局搜索（V1.0）

| 属性 | 说明 |
|---|---|
| 唤起 | `⌘⇧F` |
| 范围 | 当前工作区所有 Markdown 文件 |
| 展示 | 文件名 + 行号 + 匹配文本（高亮） |
| 操作 | 点击打开文件并定位 |
| 验收 | 500 文件搜索 < 3s |

---

### M13 导出与分享

**V3.0 变更：移除 DOCX 导出。**

#### M13-01 导出 PDF（V0.5）

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 导出 → PDF」/ `⌘⇧E` |
| 选项 | 页面大小（A4/A3/Letter）、边距、目录、页码、页眉页脚 |
| 渲染 | 基于预览区渲染结果 |
| 验收 | PDF 与预览一致；公式和图表正确 |

#### M13-02 导出 HTML（V0.5）

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 导出 → HTML」 |
| 选项 | 独立文件（内联 CSS/JS）或带资源文件夹 |
| 验收 | 浏览器中效果与编辑器一致 |

#### M13-03 导出图片（V1.0）

| 属性 | 说明 |
|---|---|
| 格式 | PNG / JPEG |
| 选项 | 背景透明（PNG）、分辨率倍数（1x/2x/3x）、裁剪空白 |
| 验收 | Retina 2x/3x 无锯齿 |

#### M13-04 复制为 HTML（V0.5）

- `⌘⇧C`；复制渲染后 HTML（含内联样式）到剪贴板。

#### M13-05 复制为纯文本（V0.5）

- `⌘⌥C`；去除 Markdown 标记的纯文本。

#### M13-06 打印（V0.5）

- `⌘P`；macOS 打印对话框。

> **DOCX 导出已移除。** 如未来需要，建议使用 Pandoc，不自研转换器。

---

### M14 写作辅助（V1.5）

#### M14-01 基础统计（V0.1，提前到 V0.1）

- 字数、字符数、段落数、阅读时间、写作时间；实时更新。

#### M14-02 专注模式（V1.0）

- 隐藏侧边栏和工具栏；非当前段落透明度 30%；`Esc` 退出。

#### M14-03 番茄钟（V1.5）

- 25 分钟工作 + 5 分钟休息；倒计时显示；完成时系统通知。

#### M14-04 写作目标（V1.5）

- 每日字数目标；进度条；达成通知。

---

### M15 设置与快捷键

#### M15-01 通用设置

| 设置项 | 默认 |
|---|---|
| 启动时打开 | 上次文件 |
| 默认编码 | UTF-8 |
| 默认换行符 | LF |
| 语言 | 跟随系统 |
| CJK fallback 字体 | 苹方 |
| 图片复制策略 | 工作区 assets |
| Markdown 兼容性 | FreeMarkdown Extended |

#### M15-02 编辑器设置

| 设置项 | 默认 |
|---|---|
| 显示行号 | 开 |
| 自动换行 | 开 |
| Tab 大小 | 2 |
| 空格替代 Tab | 开 |
| 代码折叠 | 开 |
| 光标样式 | 竖线 |
| 编辑区字体大小 | 14px |

#### M15-03 预览与渲染设置

| 设置项 | 默认 |
|---|---|
| 预览实时刷新 | 开 |
| 预览区字体大小 | 16px |
| 数学公式引擎 | KaTeX |
| Mermaid 主题跟随 | 开 |
| 脚注渲染 | 开 |
| 任务列表可交互 | 开 |
| Emoji 渲染 | 开 |
| Iconify 渲染 | 开 |
| 自动链接 | 开 |
| 换行渲染 | soft |

#### M15-04 快捷键自定义

- 设置面板查看和修改所有快捷键；冲突检测；单独/全部重置；导入/导出 JSON。

#### M15-05 快捷键总表（已修复全部冲突）

**全局快捷键**

| 快捷键 | 功能 |
|---|---|
| `⌘N` | 新建文件（新 Tab） |
| `⌘O` | 打开文件 |
| `⌘S` | 保存 |
| `⌘⇧S` | 另存为 |
| `⌘W` | 关闭 Tab/窗口 |
| `⇧⌘T` | 重新打开关闭的 Tab |
| `⌘Q` | 退出 |
| `⌘P` | 打印 |
| `⌘F` | 文档内搜索 |
| `⌘⌥F` | 替换 |
| `⌘⇧F` | 全局搜索 |
| `⌃⌘F` | **全屏**（已从 `⌘⇧F` 修复） |
| `⌘Z` / `⌘⇧Z` | 撤销/重做 |
| `⌘⇧P` | 命令面板 |
| `⌘,` | 设置 |

**编辑模式（已与 Tab 切换分离）**

| 快捷键 | 功能 |
|---|---|
| `⌘1` | 仅编辑模式 |
| `⌘2` | 仅预览模式 |
| `⌘3` | 分屏对比模式 |
| `⌘4` | 所见即所得模式（V1.0） |

**Tab 切换（已改用 `⌘⌥` 前缀，不再与编辑模式冲突）**

| 快捷键 | 功能 |
|---|---|
| `⌘⌥→` | 下一个 Tab |
| `⌘⌥←` | 上一个 Tab |
| `⌘⌥1` ~ `⌘⌥9` | 切换到第 1-9 个 Tab |
| `⌘⇧[` / `⌘⇧]` | 上/下一个 Tab（兼容） |

**视图**

| 快捷键 | 功能 |
|---|---|
| `⌘\` | 切换侧边栏 |
| `⌘⌥T` | **切换主题**（已从 `⌘⇧T` 修复） |
| `⌘+` / `⌘-` | 放大/缩小字体 |
| `⌘0` | 重置字体大小 |

**格式化**

| 快捷键 | 功能 |
|---|---|
| `⌘B` | 加粗 |
| `⌘I` | 斜体 |
| `⌘E` | 行内代码 |
| `⌘K` | 插入链接 |
| `⌘T` | 插入表格 |
| `⌘⇧X` | 删除线 |
| `⌘⇧L` | 任务列表 |
| `⌘⇧C` | 复制为 HTML |
| `⌘⌥C` | 复制为纯文本 |
| `⌘⇧E` | 导出 |
| `⌘⇧]` / `⌘⇧[` | 标题层级升降级 |
| `Tab` / `⇧Tab` | 列表缩进/反缩进 |

> **冲突修复说明：**
> - `⌘1-⌘4`：编辑模式专用；Tab 切换改用 `⌘⌥1-⌘⌥9`。
> - `⌘⇧F`：全局搜索专用；全屏改为 `⌃⌘F`。
> - `⌘⇧T`：重新打开 Tab 专用；主题切换改为 `⌘⌥T`。

---

### M16 macOS 原生集成

#### M16-01 系统外观集成（V0.1）

- Dark/Light 跟随系统；系统字体；全屏；Stage Manager 兼容；多显示器；减少动态效果；增加对比度。

#### M16-02 文件系统集成（V0.1 + V1.0）

| 属性 | 版本 | 说明 |
|---|---|---|
| Finder 拖拽 | V0.1 | 拖拽文件到窗口 |
| 文件关联 | V0.1 | `.md` 关联到本应用 |
| Quick Look | V1.0 | Quick Look 预览 `.md` |
| iCloud Drive | V1.0 | 打开/保存 iCloud 文件 |
| 最近文件 | V0.1 | macOS 菜单栏集成 |

#### M16-03 剪贴板集成（V0.1）

- `⌘C` 复制源码；`⌘V` 粘贴文本/图片；`⌘⇧C` 复制 HTML；`⌘⌥C` 复制纯文本；富文本粘贴自动转 Markdown。

#### M16-04 可访问性（V1.0）

- VoiceOver 支持；Tab 导航；WCAG 2.1 AA 对比度；减少动态效果；增加对比度。

---

### M17 外部文件修改检测（V0.5，新增 P0）

**这是 V3.0 的重要新增模块。**

#### M17-01 文件变化检测与处理

| 属性 | 说明 |
|---|---|
| 场景 | 编辑器打开 `README.md`，外部程序（VS Code/Git 等）修改了该文件 |
| 检测 | Tauri fs watch 监听文件变化 |
| 触发条件 | 文件的 mtime 或内容发生变化 |
| 处理逻辑 | 见下方决策树 |

**处理决策树：**

```
文件被外部修改
  ↓
当前文档是否有未保存修改？
  ├── 否 → 自动重新加载（静默）
  └── 是 → 弹窗：
      ┌──────────────────────────────────┐
      │  文件已被外部程序修改              │
      │                                  │
      │  README.md 在编辑器外被修改。      │
      │  您有未保存的修改。                │
      │                                  │
      │  [重新加载]  [保留当前修改]  [比较差异] │
      └──────────────────────────────────┘
        ↓              ↓              ↓
      用磁盘版本      保留编辑器版本   打开 Diff 视图
      覆盖编辑器      （可能丢失外部修改）
```

| 选项 | 行为 |
|---|---|
| 重新加载 | 用磁盘文件内容替换编辑器内容；丢弃编辑器修改 |
| 保留当前修改 | 忽略外部修改；下次保存时覆盖磁盘文件 |
| 比较差异 | 打开 Diff 视图对比编辑器版本与磁盘版本 |

**额外规则：**
- 文件被外部删除时弹窗提示（`文件已被删除，是否保留编辑器内容？`）。
- 文件被外部重命名/移动时提示。
- 设置中可关闭自动检测（但不推荐）。

**验收：**
- VS Code 修改文件后 1s 内弹出提示。
- 自动重载后光标尽量保持原位置。
- Diff 视图正确高亮差异。

---

### M18 Undo/Redo 设计（V0.1，新增 P0）

**这是 V3.0 的重要新增模块。**

#### M18-01 撤销/重做栈设计

| 属性 | 说明 |
|---|---|
| 操作 | `⌘Z` 撤销；`⌘⇧Z` 重做 |
| 独立性 | 每个 Tab 独立维护撤销栈 |
| 深度 | 最大 1000 步 |
| 粒度 | 按词组/操作粒度（非每次按键） |
| 模式切换 | 编辑模式切换不清空 Undo Stack |
| 光标 | 撤销后恢复光标位置和滚动位置 |
| 跨段落 | 跨段落撤销正确恢复 |
| WYSIWYG 兼容 | WYSIWYG 模式下操作底层仍为源码修改，撤销栈天然正确 |

**验收：**
- 1000 步内可正确撤销/重做。
- 切换编辑模式后撤销栈保留。
- 撤销后光标位置正确。

---

### M19 命令面板（V1.0，新增 P1）

**类似 VS Code / Raycast 的命令面板。**

#### M19-01 命令面板

| 属性 | 说明 |
|---|---|
| 唤起 | `⌘⇧P` |
| 布局 | 屏幕中央弹出搜索框 + 结果列表 |
| 功能 | 搜索并执行所有命令 |
| 模糊搜索 | 支持模糊匹配命令名 |

**内置命令示例：**

```
> export pdf          → 导出 PDF
> export html         → 导出 HTML
> switch theme        → 切换主题
> toggle sidebar      → 切换侧边栏
> open recent         → 打开最近文件
> new file            → 新建文件
> new window          → 新建窗口
> insert table        → 插入表格
> insert mermaid      → 插入 Mermaid 图表
> format document     → 格式化文档
> toggle preview      → 切换预览模式
> encode reload       → 重新以编码打开
> toggle lint         → 切换 Markdown Lint
```

| 属性 | 说明 |
|---|---|
| 扩展 | V2.0 插件系统可注册自定义命令 |
| 历史 | 记录最近使用的命令，置顶显示 |
| 验收 | 模糊搜索准确；命令执行 < 100ms |

---

### M20 Markdown Lint（V1.0，新增 P1）

#### M20-01 Markdown 规范检查

| 属性 | 说明 |
|---|---|
| 引擎 | markdownlint (Node.js) |
| 启用 | 设置中可开关 |
| 展示 | 编辑区中违规位置波浪下划线 + 状态栏显示问题数 |
| 面板 | 底部或侧边面板列出所有问题（规则名 + 描述 + 行号） |
| 操作 | 点击问题项跳转到对应位置 |
| 修复 | 支持一键自动修复（可修复的规则） |

**预置规则：**

| 规则 | 说明 | 可自动修复 |
|---|---|---|
| MD001 | 标题层级跳跃（如 H1 直接到 H3） | 否 |
| MD009 | 行尾多余空格 | 是 |
| MD013 | 行长度过长（可配置阈值，默认关闭） | 否 |
| MD022 | 标题上下缺少空行 | 是 |
| MD025 | 文档内多个 H1 | 否 |
| MD032 | 列表前后缺少空行 | 是 |
| MD040 | 代码块缺少语言标注 | 否 |
| MD047 | 文件末尾缺少空行 | 是 |

| 属性 | 说明 |
|---|---|
| 配置 | 设置中可逐条开关规则 |
| 兼容性 | Strict CommonMark 模式下 lint 更严格 |
| 验收 | 违规位置正确标记；自动修复不破坏语义 |

---

### M21 文件 Diff（V1.5，新增 P1）

#### M21-01 Markdown 文件对比

| 属性 | 说明 |
|---|---|
| 功能 | 对比两个 Markdown 文件的差异 |
| 触发 | 命令面板 `> compare files` / 菜单「工具 → 文件对比」 |
| 选择 | 弹出文件选择器选择两个文件 |
| 展示 | 左右分屏展示两个文件，行级 Diff 高亮 |
| Diff 类型 | 行级 Diff（增/删/改不同颜色高亮） |
| 渲染 Diff | 可选：切换到渲染后 Markdown 的 Diff（对比渲染效果差异） |
| 同步滚动 | 左右分屏同步滚动 |
| 验收 | 差异行正确高亮；大文件 Diff < 1s |

---

### M22 文档模板系统（V1.0，新增 P1）

#### M22-01 内置模板

| 模板名 | 内容结构 |
|---|---|
| 空白 | — |
| README | 标题 + 简介 + 安装 + 使用 + 贡献 + 许可证 |
| 技术设计文档 | 背景 + 需求 + 架构 + 接口 + 数据模型 + 部署 |
| PRD 文档 | 产品背景 + 用户需求 + 功能设计 + 非功能需求 + 验收标准 |
| 会议纪要 | 日期 + 参会人 + 议题 + 决议 + 待办 |
| API 文档 | 接口名 + 请求 + 响应 + 参数 + 示例 |

#### M22-02 模板管理

| 属性 | 说明 |
|---|---|
| 位置 | `~/Library/Application Support/FreeMarkdown/templates/` |
| 新建 | 新建文件时可选择模板 |
| 自定义 | 用户可将任意 `.md` 文件保存为模板 |
| 管理 | 设置 → 模板管理：查看/删除/重命名自定义模板 |
| 验收 | 模板列表正确展示；选择后内容正确插入 |

---

### M23 大文件降级策略（V0.5，新增 P0）

**V2.0 问题：10MB 文件全量渲染 Mermaid+KaTeX+Shiki 不现实。V3.0 增加性能分级策略。**

#### M23-01 文档规模性能分级

| 文档规模 | 策略 | 说明 |
|---|---|---|
| < 1MB | 实时全量预览 | 全部功能正常；预览延迟 < 100ms |
| 1–5MB | 增量渲染 | 仅渲染可视区域 + 预渲染上下文；Mermaid/KaTeX 按需渲染 |
| 5–10MB | 虚拟滚动 | 编辑区和预览区均启用虚拟滚动；仅渲染可视行 |
| > 10MB | Large File Mode | 自动降级模式（见下方） |

#### M23-02 Large File Mode（大文件模式）

当文件 > 10MB 时自动启用：

| 关闭 | 说明 |
|---|---|
| Mermaid 实时渲染 | 图表显示为源码，点击时渲染 |
| KaTeX 实时渲染 | 公式显示为源码，点击时渲染 |
| 图片预加载 | 图片懒加载到可视区域 |
| 全量语法高亮 | 代码高亮降级为简单着色 |
| 实时预览 | 预览改为手动刷新 |

| 开启 | 说明 |
|---|---|
| 虚拟滚动 | 仅渲染可视区域行 |
| 按需渲染 | 滚动停止后渲染可视区域内容 |
| 状态栏提示 | 显示「Large File Mode」标记 |

| 属性 | 说明 |
|---|---|
| 手动切换 | 用户可在状态栏点击 Large File Mode 标记手动开关 |
| 阈值配置 | 设置中可配置触发阈值（默认 10MB） |
| 验收 | 10MB 文件可正常编辑不卡顿；虚拟滚动无明显延迟 |

---

## 7. 版本规划与开发里程碑

### V0.1 Alpha（核心验证）

**目标：验证编辑器核心能力。**

| 模块 | 功能 |
|---|---|
| 编辑器 | Markdown 源码编辑（CodeMirror 6） |
| 预览 | 实时双栏预览（markdown-it） |
| 布局 | 仅编辑 / 仅预览 / 分屏对比 |
| 文件 | 打开、保存、自动保存、最近文件 |
| 多 Tab | 新建、关闭、切换、拖拽排序 |
| 编码 | UTF-8 |
| Markdown | CommonMark + GFM |
| 代码 | Shiki 语法高亮 + 复制按钮 |
| 主题 | 3 个预置主题（办公白/夜间深色/程序员阅读）+ Dark/Light 跟随 |
| Undo/Redo | 每 Tab 独立栈，1000 步 |
| macOS | 原生窗口、Finder 拖拽、文件关联 |
| 统计 | 字数、字符数、行数 |

**里程碑：可编辑 Markdown 文件并实时预览。**

### V0.5 Beta（核心完善）

| 模块 | 功能 |
|---|---|
| 编码 | GBK / GB18030 / Big5 / Shift-JIS / EUC-KR + 自动识别 + 置信度 |
| CJK | 字体 fallback |
| 扩展 | Mermaid 图表 + KaTeX 公式 |
| 搜索 | 文档内搜索替换（正则） |
| 导出 | PDF、HTML、复制为 HTML/纯文本 |
| 文件恢复 | 异常退出草稿恢复 |
| 外部检测 | 外部文件修改检测（M17） |
| 大文件 | 降级策略（M23） |
| 代码增强 | 行号、折叠、高亮指定行 |

**里程碑：可用于实际技术文档编辑。**

### V1.0 正式版（产品级）

| 模块 | 功能 |
|---|---|
| 编辑模式 | 所见即所得（CodeMirror decorations）、沉浸式写作 |
| 窗口 | 多窗口支持 |
| 主题 | 完整 6 主题 + 代码高亮主题独立 |
| 扩展 | TOC、脚注、任务列表、Emoji、Iconify |
| 图片 | 拖拽、粘贴、管理器、编辑（4 种复制策略） |
| 工作区 | 文件夹工作区 |
| 搜索 | 全局搜索 |
| 导出 | 导出图片 |
| 快捷键 | 自定义 |
| macOS | Quick Look、iCloud Drive |
| 命令面板 | `⌘⇧P` |
| Markdown Lint | 规范检查 + 自动修复 |
| 文档模板 | 内置 6 模板 + 自定义 |
| 可访问性 | VoiceOver、WCAG AA |

**里程碑：可公开发布的正式版。**

### V1.5（增强）

| 模块 | 功能 |
|---|---|
| 主题 | 自定义主题编辑器、主题导入/导出 |
| 版本历史 | 智能快照、恢复到指定版本 |
| Tab 分屏 | 垂直/水平分屏对比 |
| 文件 Diff | 两个文件对比 |
| 写作辅助 | 番茄钟、写作目标 |
| 固定 Tab | Tab 缩小为图标 |

### V2.0（远期）

| 模块 | 功能 |
|---|---|
| MDX | MDX 支持 |
| Git | 集成（查看 diff、提交） |
| 插件系统 | 扩展 API |
| 主题市场 | 社区分享 |
| 版本历史 | diff-based 增量存储 |

### V3.0（AI 能力）

| 功能 | 说明 |
|---|---|
| AI 写作辅助 | 改写、总结、扩写、翻译、润色 |
| AI Markdown 助手 | 自然语言生成 Markdown |
| 本地 AI | Ollama / LM Studio / OpenAI Compatible API |
| 定位 | 完全本地 Markdown + 本地 AI |

---

## 8. 非功能需求

### 8.1 性能指标（含大文件降级）

| 指标 | 目标 |
|---|---|
| 冷启动 | < 1.5s |
| 普通文档打开 | < 500ms |
| 1MB 文件打开 | < 2s |
| 10MB 文件 | 正常编辑（Large File Mode 自动启用） |
| 10,000 行 | 流畅编辑 |
| 预览渲染延迟 | < 100ms（< 1MB 文档） |
| 内存占用 | < 300MB（单文档） |
| 搜索响应 | < 200ms（10万行） |
| 主题切换 | < 300ms |
| Tab 切换 | < 50ms |

### 8.2 兼容性

- macOS 13 Ventura+
- Apple Silicon (arm64) 优先 + Intel (x86_64) 兼容

### 8.3 安全性

- 不上传用户文档（纯本地）。
- 外部链接可设二次确认。
- 文件写入仅在用户操作下触发。
- 禁用 `<script>` `<iframe>` 等标签。
- HTML 渲染白名单。

### 8.4 本地化

- 简体中文、English；跟随系统区域设置；中文输入法下不触发 Markdown 快捷键；CJK 字体 fallback。

### 8.5 持久化

| 数据类型 | 位置 | 格式 |
|---|---|---|
| 应用设置 | `~/Library/Application Support/FreeMarkdown/` | `settings.json` |
| 自定义主题 | 同上 `/themes/` | `*.fmtheme` |
| 快捷键配置 | 同上 | `keybindings.json` |
| 草稿 | 同上 `/drafts/` | 临时 `.md` |
| 版本历史 | 同上 `/versions/` | 快照 |
| 模板 | 同上 `/templates/` | `.md` |
| 最近文件 | 同上 | `recent.json` |

---

## 9. 竞品差异化分析

| 产品 | 优势 | 不足 | 本产品差异化 |
|---|---|---|---|
| Typora | 易用、WYSIWYG 成熟 | 收费、定制有限 | 免费且可深度定制 |
| Obsidian | 强大、插件生态 | 更像知识库，非纯编辑器 | 纯编辑器定位，开箱即用 |
| MarkText | 免费、开源 | UI 与 macOS 体验一般 | Tauri 2 原生体验更优 |
| VS Code | 强大、扩展强 | 太重，非专注编辑器 | 轻量专注 |
| MacDown | 免费、开源 | 功能传统，UI 老旧 | 现代 UI + 多 Tab + 多编码 |

### 核心竞争力（只抓 5 件事）

1. **macOS 原生体验**——Tauri 2 原生窗口 + 系统集成
2. **Markdown 编辑体验**——CodeMirror 6 + markdown-it + KaTeX + Mermaid + Shiki
3. **多编码兼容**——UTF-8/GBK/GB18030/Big5/Shift-JIS/EUC-KR + 自动识别 + 转换
4. **中文/CJK 排版**——CJK 字体 fallback + 中英文混排优化
5. **免费开源**——MIT 许可证

---

## 10. 风险清单

| 编号 | 风险 | 等级 | 缓解措施 |
|---|---|---|---|
| R01 | WYSIWYG (CodeMirror decorations) 实现复杂度 | 高 | 延后至 V1.0；V0.1 先做纯源码 + 双栏预览；decoration 失败时降级为纯源码 |
| R02 | Tauri 2 生态成熟度（部分 API 可能不稳定） | 中 | 优先使用稳定 API；关注 Tauri 2 更新；必要时用 Rust 原生实现替代 |
| R03 | Rust 工具链未安装 | 低 | 开发前安装 Rust (`rustup`)；验证 `cargo build` 通过 |
| R04 | 大文件渲染性能 | 中 | 分级策略（M23）；Large File Mode 自动降级 |
| R05 | 编码检测准确率（短文本） | 中 | 置信度机制 + 用户手动选择 |
| R06 | 外部文件修改检测误报 | 低 | 仅在 mtime 变化时触发；文件内容 hash 对比确认 |
| R07 | 多 Tab 内存增长 | 中 | 限制最大 Tab 数（20）；非活跃 Tab 释放渲染资源 |
| R08 | Mermaid/KaTeX 渲染阻塞 | 中 | 异步渲染；按需渲染（仅可视区域）；Web Worker |
| R09 | 图片复制策略用户困惑 | 低 | 4 种策略可选 + 「记住选择」；默认工作区模式 |
| R10 | 主题系统过度复杂 | 低 | V1.5 精简版先行；复杂参数（渐变/模糊）降为 P2 |

---

## 11. 验收清单

### V0.1 Alpha

- [ ] Markdown 源码编辑语法高亮正确
- [ ] 双栏预览 < 100ms 延迟
- [ ] 文件打开/保存/最近文件正常
- [ ] 多 Tab 新建/关闭/切换/拖拽排序正常
- [ ] 3 个预置主题可切换
- [ ] Dark/Light 跟随系统
- [ ] Undo/Redo 每 Tab 独立，1000 步
- [ ] Shiki 代码高亮 + 复制按钮
- [ ] macOS 原生窗口 + Finder 拖拽
- [ ] 字数统计正确

### V0.5 Beta

- [ ] GBK/GB18030 文件正确打开无乱码
- [ ] 编码检测置信度机制正常
- [ ] CJK 字体 fallback 正确
- [ ] Mermaid 全图表类型渲染正确
- [ ] KaTeX 行内/块级公式渲染正确
- [ ] 文档内搜索替换（正则）正常
- [ ] 导出 PDF/HTML 正确
- [ ] 复制为 HTML/纯文本正常
- [ ] 异常退出后文件恢复正常
- [ ] 外部文件修改检测弹窗正常
- [ ] 大文件（10MB）Large File Mode 自动启用

### V1.0 正式版

- [ ] WYSIWYG 模式渲染正确，切回源码保留语法
- [ ] 沉浸写作模式非当前段落弱化
- [ ] 多窗口独立运行正常
- [ ] 6 个预置主题 + 代码高亮主题独立切换
- [ ] TOC 自动生成且可跳转
- [ ] 脚注正确渲染且可跳转
- [ ] Emoji `:shortcode:` 正确渲染
- [ ] Iconify 图标正确渲染
- [ ] 图片拖拽/粘贴/管理器正常
- [ ] 图片 4 种复制策略可选
- [ ] 全局搜索正常
- [ ] 命令面板模糊搜索 + 执行正常
- [ ] Markdown Lint 规则检查 + 自动修复正常
- [ ] 文档模板 6 个内置 + 自定义正常
- [ ] 快捷键自定义 + 冲突检测正常
- [ ] 快捷键无冲突（⌘1-4 编辑模式、⌘⌥1-9 Tab、⌃⌘F 全屏、⌘⌥T 主题）
- [ ] VoiceOver 可读编辑区和预览区

---

## 12. 变更记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| V1.0 | 2026-09-03 | 初版需求说明书，10 个功能模块 |
| V2.0 | 2026-09-03 | 扩展至 16 个功能模块；新增 4 种编辑模式、多 Tab/多窗口、多编码、CJK fallback、写作辅助、版本历史、Iconify、学术主题、代码高亮主题独立、macOS 原生集成、竞品分析、AI 路线图 |
| V3.0 | 2026-09-03 | 架构重构与 MVP 收敛；确认 Tauri 2 + React 技术方案；移除 DOCX 导出；版本规划重构为 V0.1/V0.5/V1.0/V1.5/V2.0/V3.0 渐进式；新增 M17-M23 共 7 个模块（外部文件检测、Undo/Redo 设计、命令面板、Markdown Lint、文件 Diff、文档模板、大文件降级）；修复全部快捷键冲突；自动保存简化为 2 种模式；版本历史改为智能快照；图片复制策略改为 4 选项；Markdown 兼容性分层；编码检测置信度机制；WYSIWYG 确认采用 CodeMirror decorations 方案；核心用户聚焦；功能优先级矩阵；风险清单 |
