# FreeMarkdown — macOS Markdown 编辑器产品需求说明书

| 字段 | 值 |
|---|---|
| 文档版本 | V2.0 |
| 前置版本 | V1.0（2026-09-03） |
| 创建日期 | 2026-09-03 |
| 产品名称 | FreeMarkdown（暂定，候选名：MarkFlow / Ink / Paper / MNote） |
| 目标平台 | macOS 13 Ventura 及以上，Apple Silicon 优先 + Intel 兼容 |
| 授权模式 | 完全免费，开源（MIT 或同类许可证） |
| 文档状态 | 发布 |

---

## 变更摘要（V1.0 → V2.0）

| 变更类型 | 内容 |
|---|---|
| **新增** | 4 种编辑模式（纯源码 / 双栏预览 / 所见即所得 / 沉浸写作）；多 Tab 体系（拖拽排序 / 分屏 / 快捷键）；多窗口支持；多编码（UTF-8/GBK/GB18030/UTF-16 等 + 自动识别 + 转换）；CJK 字体 fallback；写作辅助（番茄钟 / 写作目标 / 写作统计）；版本历史（文件快照）；Iconify 图标支持；学术/论文主题；代码高亮主题（独立于编辑器主题）；macOS 原生集成（Stage Manager / Quick Look / iCloud Drive / 多显示器）；全局搜索；工作区概念；DOCX 导出；图片资源管理器；竞品差异化分析；AI 能力路线图 |
| **重构** | 功能模块从 10 个扩展至 16 个；主题系统重构（6 预置主题 + 独立代码高亮主题）；技术架构新增 Swift 原生方案对比；版本规划拆分为 V1.0 MVP / V1.5 / V2.0 / V3.0 AI |
| **细化** | 表格可视化编辑（增删行列 / 拖拽列宽 / 对齐）；图片编辑（调整大小 / 裁剪 / 标题）；超链接增强（URL 卡片 / 本地文件链接）；未保存文件管理（三种自动保存策略）；文件恢复流程 |

---

## 目录

- [1. 产品概述](#1-产品概述)
- [2. 产品信息架构](#2-产品信息架构)
- [3. 功能模块总览](#3-功能模块总览)
- [4. 详细需求](#4-详细需求)
  - [M01 界面与布局](#m01-界面与布局)
  - [M02 编辑模式](#m02-编辑模式)
  - [M03 多 Tab 与多窗口](#m03-多-tab-与多窗口)
  - [M04 主题体系](#m04-主题体系)
  - [M05 自定义主题](#m05-自定义主题)
  - [M06 Markdown 标准格式](#m06-markdown-标准格式)
  - [M07 扩展格式（公式/图表/目录等）](#m07-扩展格式)
  - [M08 代码支持](#m08-代码支持)
  - [M09 图片功能](#m09-图片功能)
  - [M10 超链接与图标](#m10-超链接与图标)
  - [M11 文件管理与编码](#m11-文件管理与编码)
  - [M12 搜索功能](#m12-搜索功能)
  - [M13 导出与分享](#m13-导出与分享)
  - [M14 写作辅助](#m14-写作辅助)
  - [M15 设置与快捷键](#m15-设置与快捷键)
  - [M16 macOS 原生集成](#m16-macos-原生集成)
- [5. 非功能需求](#5-非功能需求)
- [6. 技术架构](#6-技术架构)
- [7. 竞品差异化分析](#7-竞品差异化分析)
- [8. 版本规划](#8-版本规划)
- [9. 验收清单](#9-验收清单)
- [10. 变更记录](#10-变更记录)

---

## 1. 产品概述

### 1.1 产品定位

一款面向 macOS 的**免费、原生、高性能、多语言、多编码、支持多文件工作区**，并提供高度自由排版与主题定制能力的现代 Markdown 编辑器。

核心理念：**简洁而不简单，开箱即用，同时允许深度定制。**

### 1.2 产品一句话定义

Mac 上最美观、最自由、最适合长期写作和技术文档的免费 Markdown 编辑器。

### 1.3 目标用户

| 用户角色 | 核心诉求 |
|---|---|
| 程序员 | 代码高亮、暗色主题、Mermaid 流程图、数学公式、Git 集成、快捷键 |
| 产品经理 | PRD 文档、流程图、思维导图、表格、图片、导出 PDF/HTML |
| 技术写作者 | 长文排版、目录自动生成、导出 PDF/HTML、沉浸写作 |
| 学生 / 研究人员 | LaTeX 数学公式、文献链接、表格、PDF 导出、目录结构 |
| 博客作者 | 沉浸式写作、专注模式、字体主题、字数统计、夜间模式 |
| 日常办公用户 | 所见即所得、表格美化、图片缩放、导出 Word/PDF |
| AI 辅助写作用户 | 本地 AI 写作辅助、AI 润色/翻译/总结（V3.0 路线图） |

### 1.4 非目标（本期不覆盖）

- 不做云端同步 / 在线协作（V3.0 规划 iCloud）。
- 不做移动端（iOS/iPadOS）版本。
- 不做插件市场（V2.0 保留扩展 API，不运营市场）。

---

## 2. 产品信息架构

```
FreeMarkdown
│
├── 文件管理
│   ├── 最近文件
│   ├── 本地文件
│   ├── 文件夹 / 工作区
│   ├── 多 Tab（拖拽 / 排序 / 分屏）
│   ├── 多窗口
│   └── 文件恢复 / 版本历史
│
├── 编辑器
│   ├── 纯 Markdown 源码模式
│   ├── 实时双栏预览模式
│   ├── 所见即所得模式（WYSIWYG）
│   ├── 沉浸式写作模式
│   └── 分屏对比
│
├── 文档结构
│   ├── 自动目录（TOC）
│   ├── 大纲导航
│   ├── 文档内搜索
│   └── 全局搜索
│
├── 主题系统
│   ├── 预置场景主题（6 套）
│   ├── 代码高亮主题（独立）
│   ├── 自定义主题编辑器
│   └── 主题导入 / 导出
│
├── Markdown 扩展
│   ├── 数学公式（KaTeX）
│   ├── Mermaid 图表
│   ├── 代码块（200+ 语言）
│   ├── 表格（可视化编辑）
│   ├── 图片（管理 / 编辑）
│   ├── 超链接（URL 卡片）
│   ├── 图标（Emoji / Iconify）
│   ├── 目录 / 脚注 / 任务列表
│   └── HTML 安全子集
│
├── 编码与国际化
│   ├── 多编码（UTF-8 / GBK / GB18030 / UTF-16 等）
│   ├── 自动编码识别
│   ├── 编码转换
│   └── CJK 字体 fallback
│
├── 写作辅助
│   ├── 字数 / 字符 / 段落统计
│   ├── 阅读时间估算
│   ├── 番茄钟
│   └── 写作目标
│
├── 导出
│   ├── Markdown
│   ├── HTML
│   ├── PDF
│   ├── DOCX
│   └── 图片（PNG / JPEG）
│
└── 设置中心
    ├── 通用 / 编辑器 / 预览
    ├── 主题 / 代码 / 图片
    ├── Markdown 渲染
    ├── 导出
    ├── 快捷键自定义
    └── 关于
```

---

## 3. 功能模块总览

| 编号 | 功能模块 | 功能点数 | 优先级 |
|---|---|---|---|
| M01 | 界面与布局 | 10 | P0 |
| M02 | 编辑模式 | 4 | P0 |
| M03 | 多 Tab 与多窗口 | 8 | P0 |
| M04 | 主题体系 | 8 | P0 |
| M05 | 自定义主题 | 10 | P0 |
| M06 | Markdown 标准格式 | 14 | P0 |
| M07 | 扩展格式（公式/图表/目录等） | 10 | P0 |
| M08 | 代码支持 | 5 | P0 |
| M09 | 图片功能 | 4 | P1 |
| M10 | 超链接与图标 | 3 | P0 |
| M11 | 文件管理与编码 | 8 | P0 |
| M12 | 搜索功能 | 2 | P0 |
| M13 | 导出与分享 | 7 | P0 |
| M14 | 写作辅助 | 4 | P1 |
| M15 | 设置与快捷键 | 5 | P1 |
| M16 | macOS 原生集成 | 4 | P0 |

---

## 4. 详细需求

### M01 界面与布局

#### M01-01 窗口与标题栏

| 属性 | 说明 |
|---|---|
| 操作/方法 | macOS 原生窗口，支持左上角红绿灯按钮（关闭/最小化/全屏） |
| 规则 | 标题栏显示当前文件名 + 修改标记（`●`）；未保存文件显示 `未命名` |
| 快捷键 | `⌘W` 关闭窗口/Tab；`⌘M` 最小化 |
| 验收 | 窗口拖拽正常；全屏后退出恢复原大小；多窗口独立 |

#### M01-02 布局模式切换

| 模式 | 说明 | 快捷键 |
|---|---|---|
| 仅编辑 | 全屏源码编辑 | `⌘1` |
| 仅预览 | 全屏渲染预览 | `⌘2` |
| 分屏对比 | 左编辑右预览，实时同步滚动 | `⌘3`（默认） |

- 分屏模式下拖动中间分隔条可调整编辑区与预览区的宽度比例（3:7 ~ 7:3）。
- 分隔条位置持久化记忆。
- 编辑区与预览区双向滚动同步。

#### M01-03 侧边栏（文件树 / 工作区）

| 属性 | 说明 |
|---|---|
| 功能 | 展示已打开文件夹/工作区的文件树，支持 Markdown 文件快速切换 |
| 操作 | 点击文件名打开；右键菜单：新建、重命名、删除、在 Finder 中显示、复制路径 |
| 收起/展开 | `⌘\` 切换侧边栏显示 |
| 搜索 | 侧边栏顶部搜索框，按文件名实时过滤 |
| 验收 | 大目录（500+ 文件）加载 < 1s；树形展开/折叠有动画 |

#### M01-04 工具栏

| 属性 | 说明 |
|---|---|
| 位置 | 窗口顶部，标题栏下方 |
| 内容 | 布局切换、主题切换、快速格式按钮（加粗/斜体/链接/图片/表格/代码块）、导出、设置 |
| 可定制 | 用户可隐藏/显示工具栏，设置中配置按钮可见性 |
| 验收 | 按钮有 hover/active 状态；暗色主题下图标清晰 |

#### M01-05 状态栏

| 属性 | 说明 |
|---|---|
| 位置 | 窗口底部 |
| 显示内容 | 字数、字符数、行数、列数、光标位置（行:列）、阅读时间、编码格式、保存状态 |
| 示例 | `Words: 1,234  Characters: 6,720  Lines: 120  Reading: 5 min  Markdown | UTF-8 | Saved` |
| 切换 | 设置中可自定义显示项 |
| 验收 | 实时更新（输入时同步）；数字千分位格式化 |

#### M01-06 面包屑导航（大纲）

| 属性 | 说明 |
|---|---|
| 功能 | 编辑区顶部显示当前光标所在标题层级面包屑（如 `产品需求 > 用户需求 > 用户画像`） |
| 操作 | 点击某层级标题可快速跳转到该标题位置 |
| 验收 | 标题层级正确；跳转后光标定位准确 |

#### M01-07 字数统计与阅读时间

| 属性 | 说明 |
|---|---|
| 功能 | 状态栏显示：中文字数、英文词数、阅读预估时间（按 300 字/分钟） |
| 验收 | 中英文混合正确统计；阅读时间取整显示 |

#### M01-08 拖拽支持

| 属性 | 说明 |
|---|---|
| 拖拽文件 | 拖拽 `.md` 文件到窗口直接打开（新 Tab） |
| 拖拽图片 | 拖拽图片到编辑区自动插入 Markdown 图片语法，图片复制到 `assets/` 子目录 |
| 拖拽文本 | 拖拽纯文本以引用方式插入 |
| 验收 | 拖拽有视觉反馈（高亮边框）；图片自动复制到 assets 并生成相对路径 |

#### M01-09 大纲导航面板

| 属性 | 说明 |
|---|---|
| 功能 | 预览区右侧可固定显示「大纲面板」，展示当前文档的标题树 |
| 操作 | 点击标题项快速跳转到对应位置 |
| 同步 | 大纲随标题增删改实时更新 |
| 配置 | 可设置大纲最大层级（如仅显示 H1–H3） |
| 验收 | 大纲层级缩进正确；跳转后光标定位准确 |

#### M01-10 全屏与沉浸模式

| 属性 | 说明 |
|---|---|
| 操作 | `⌘⇧F` 进入全屏；全屏下隐藏侧边栏和工具栏 |
| 退出 | `Esc` 或 `⌘⇧F` 退出全屏 |
| 验收 | 全屏过渡动画流畅；退出后恢复之前布局 |

---

### M02 编辑模式

#### M02-01 纯 Markdown 源码模式

| 属性 | 说明 |
|---|---|
| 描述 | 类似 VS Code / Typora Code Mode，用户直接编辑 Markdown 源码 |
| 适用 | 程序员、技术文档、Markdown 熟练用户 |
| 特性 | 语法高亮、行号、代码折叠、自动补全 |
| 快捷键 | `⌘1` 切换到此模式 |
| 验收 | 源码语法着色正确；大文件滚动流畅 |

#### M02-02 实时双栏预览模式

| 属性 | 说明 |
|---|---|
| 描述 | 左编辑右预览，输入后 < 100ms 预览区更新 |
| 同步 | 左右同步滚动、光标定位同步 |
| 交互 | 点击预览区某位置，编辑区光标跳转到对应源码位置 |
| 宽度 | 可拖动分隔条调整左右宽度比例（3:7 ~ 7:3） |
| 适用 | 需要实时查看渲染效果的用户 |
| 快捷键 | `⌘3` 切换到此模式（默认） |
| 验收 | 输入时预览无闪烁；滚动同步无明显延迟；大文档增量渲染不卡顿 |

#### M02-03 所见即所得模式（WYSIWYG）

| 属性 | 说明 |
|---|---|
| 描述 | 用户不需要直接面对 Markdown 标记，输入 `# 标题` 后自动渲染为样式化标题，但底层仍是 Markdown |
| 行为 | 退格/输入时光标在渲染后的文本中操作；切换回源码模式可看到原始 Markdown |
| 适用 | 普通办公用户、写作者、Markdown 初学者 |
| 快捷键 | `⌘4` 切换到此模式 |
| 验收 | 输入 Markdown 语法后即时渲染为样式化文本；切换回源码模式保留原始语法；光标定位准确 |
| 备注 | 基于编辑器内核实现行内渲染（CodeMirror 6 decorations 或 ProseMirror） |

#### M02-04 沉浸式写作模式

| 属性 | 说明 |
|---|---|
| 描述 | 全屏、隐藏侧边栏和工具栏，专注当前段落 |
| 特性 | 自动弱化非当前段落（降低透明度至 30%），当前编辑段落全亮 |
| 背景 | 背景色可调（跟随主题或自定义） |
| 内容宽度 | 可设置内容最大宽度（如 600px / 800px），居中显示 |
| 退出 | `Esc` 退出 |
| 适用 | 小说、博客、长文创作 |
| 验收 | 非当前段落透明度降低；输入时仅当前段落高亮；退出后恢复正常显示 |

---

### M03 多 Tab 与多窗口

#### M03-01 多 Tab 支持

| 属性 | 说明 |
|---|---|
| 功能 | 单窗口内支持多 Tab 打开多个文件，类似 VS Code / Safari |
| 位置 | Tab 栏位于工具栏上方，标题栏下方 |
| 样式 | Tab 显示文件名 + 修改标记（`●`）；当前 Tab 高亮 |
| 限制 | 单窗口最多 20 个 Tab（超过时 Tab 栏可横向滚动） |
| 验收 | Tab 切换 < 50ms；切换后恢复光标位置和滚动位置 |

#### M03-02 Tab 新建与关闭

| 操作 | 快捷键 | 说明 |
|---|---|---|
| 新建 Tab | `⌘N` | 创建空白文档 |
| 关闭当前 Tab | `⌘W` | 有未保存修改时弹出确认对话框 |
| 重新打开已关闭 Tab | `⇧⌘T` | 恢复最近关闭的 Tab（最多记忆 10 个） |
| 关闭其他 | 右键菜单 | 关闭除当前外的所有 Tab |
| 关闭右侧全部 | 右键菜单 | 关闭当前 Tab 右侧的所有 Tab |
| 关闭全部 | 右键菜单 | 关闭所有 Tab |

Tab 右键菜单完整项：

```
关闭          ⌘W
关闭其他
关闭右侧
关闭全部
────────────
固定
复制文件路径
在 Finder 中显示
────────────
重新打开       ⇧⌘T
```

#### M03-03 Tab 拖拽排序

| 属性 | 说明 |
|---|---|
| 功能 | 用户可拖拽 Tab 重新排序 |
| 视觉 | 拖拽时 Tab 半透明跟随鼠标；目标位置显示插入指示线 |
| 验收 | 拖拽排序后 Tab 顺序持久化（同窗口内）；排序过程中不触发文件切换 |

#### M03-04 Tab 分屏对比

| 属性 | 说明 |
|---|---|
| 功能 | 拖拽 Tab 到编辑区边缘可创建分屏，同时在两个编辑区中打开不同文件 |
| 分屏方式 | 垂直分屏（左 | 右）和水平分屏（上 — 下） |
| 用途 | 对比两个文档、编辑 README 同时参考 API 文档 |
| 关闭 | 点击分屏区的关闭按钮恢复单编辑区 |
| 优先级 | P1（V1.5 实现） |
| 验收 | 分屏后两个编辑区独立滚动；各自可切换编辑/预览模式 |

#### M03-05 Tab 快捷键

| 功能 | 快捷键 |
|---|---|
| 新建 Tab | `⌘N` |
| 关闭当前 Tab | `⌘W` |
| 重新打开关闭 Tab | `⇧⌘T` |
| 下一个 Tab | `⌘⌥→` |
| 上一个 Tab | `⌘⌥←` |
| 切换到第 1-9 个 Tab | `⌘1` ~ `⌘9`（仅 Tab 模式下，否则与布局模式快捷键冲突时 Tab 优先） |

兼容：`⌘PageUp` / `⌘PageDown` 切换 Tab。

#### M03-06 多窗口支持

| 属性 | 说明 |
|---|---|
| 功能 | 支持多个独立 macOS 窗口，每个窗口有自己的 Tab 组 |
| 操作 | 菜单「窗口 → 新建窗口」/ `⌘⇧N` |
| 独立 | 每个窗口独立记住布局模式、主题、侧边栏状态 |
| 验收 | 多窗口可独立全屏；窗口间不互相干扰 |

#### M03-07 固定 Tab

| 属性 | 说明 |
|---|---|
| 功能 | 可将 Tab 固定（Pin），固定的 Tab 缩小为图标，不占完整宽度 |
| 操作 | 右键 → 固定 / 取消固定 |
| 验收 | 固定 Tab 排列在左侧；不会被「关闭其他」影响 |

#### M03-08 最终文件管理架构

```
Application
│
├── Window 1
│   ├── Tab 1 → README.md
│   ├── Tab 2 → PRD.md
│   └── Tab 3 → API.md
│
├── Window 2
│   ├── Tab 1 → Notes.md
│   └── Tab 2 → Ideas.md
│
└── Workspace
    ├── Project A
    └── Project B
```

---

### M04 主题体系

#### M04-01 预置主题列表

| 编号 | 主题名称 | 适用场景 | 特点 |
|---|---|---|---|
| T01 | **办公白**（Office Light） | 日常办公、PRD、会议记录 | 白底黑字，苹方字体，16px，行高 1.8，浅灰边框，蓝色链接 |
| T02 | **夜间深色**（Dark Night） | 夜间、低光环境 | 深灰底浅字，低蓝光，低对比度护眼 |
| T03 | **程序员阅读**（Code Reader） | 代码/技术文档阅读 | 深色背景，JetBrains Mono 等宽字体为主，One Dark 代码高亮 |
| T04 | **沉浸写作**（Focus Writer） | 小说、博客、长文创作 | 米白底 (#F7F3E8)，霞鹜文楷/思源宋体，高行距，内容宽度限制居中 |
| T05 | **极简灰**（Minimal Gray） | 极简风格 | 大量留白，隐藏边框，极简工具栏，浅灰底灰色字 |
| T06 | **学术论文**（Academic） | 论文、研究报告 | Times New Roman / 宋体，公式优化，段落缩进，窄页面 |

#### M04-02 代码高亮主题（独立于编辑器主题）

代码高亮主题与编辑器主题可独立组合，用户可自由搭配。

| 类型 | 主题名 |
|---|---|
| 浅色 | GitHub Light、VS Code Light、IntelliJ Light |
| 深色 | GitHub Dark、Dracula、Monokai、One Dark、Nord |

| 属性 | 说明 |
|---|---|
| 切换 | 设置 → 代码 → 代码高亮主题 |
| 预览 | 选中时预览代码块效果 |
| 默认 | 跟随编辑器主题（浅色主题→GitHub Light，深色主题→One Dark） |
| 验收 | 代码高亮在编辑区和预览区一致 |

#### M04-03 主题切换

| 属性 | 说明 |
|---|---|
| 操作 | 工具栏主题下拉菜单 / 设置面板 / `⌘⇧T` |
| 预览 | 主题选择面板实时预览效果（编辑区内容不变，仅样式切换） |
| 持久化 | 切换后自动保存为默认主题 |
| 动画 | 切换时有 200ms 过渡动画 |

#### M04-04 跟随系统外观

| 属性 | 说明 |
|---|---|
| 功能 | 可设置「跟随系统外观」，macOS 切换深色/浅色模式时编辑器自动切换对应主题 |
| 规则 | 用户分别配置「浅色模式主题」和「深色模式主题」 |
| 验收 | 系统切换外观时编辑器 300ms 内切换主题 |

#### M04-05 主题应用范围

| 范围 | 说明 |
|---|---|
| 编辑区 | 语法高亮配色、背景、字体 |
| 预览区 | 渲染后的 HTML 样式（标题/段落/列表/代码块/表格等） |
| UI 界面 | 工具栏、侧边栏、状态栏、面板 |
| 代码高亮 | 独立配置，可与编辑器主题自由组合 |

#### M04-06 主题管理

| 属性 | 说明 |
|---|---|
| 新建 | 从现有主题「另存为」创建自定义主题副本 |
| 编辑 | 跳转至自定义主题编辑器（见 M05） |
| 删除 | 可删除自定义主题；预置主题不可删除 |
| 导入/导出 | 支持以 `.fmtheme`（JSON 格式）导入导出主题 |
| 分享 | 导出的主题文件可分享给其他用户直接导入使用 |
| 恢复默认 | 自定义主题可一键重置为基础主题参数 |

#### M04-07 主题预览卡片

| 属性 | 说明 |
|---|---|
| 功能 | 主题选择面板中每个主题展示缩略预览卡片 |
| 内容 | 卡片展示标题、正文、代码块、表格的渲染效果色样 |
| 尺寸 | 约 200×120px；hover 时放大显示更多细节 |

#### M04-08 主题文件格式

```json
{
  "name": "My Writing Theme",
  "version": "1.0",
  "font": {
    "body": "LXGW WenKai",
    "heading": "LXGW WenKai",
    "code": "JetBrains Mono",
    "size": 18,
    "lineHeight": 1.8,
    "weight": 400,
    "letterSpacing": 0
  },
  "colors": {
    "background": "#F7F3E8",
    "text": "#333333",
    "heading": "#222222",
    "link": "#0066cc",
    "code": "#c7254e",
    "quote": "#666666"
  },
  "page": {
    "maxWidth": "800px",
    "padding": "40px"
  },
  "table": {
    "headerBg": "#f5f5f5",
    "borderColor": "#ddd",
    "zebra": true,
    "zebraColor": "#fafafa"
  },
  "image": {
    "maxWidth": "80%",
    "borderRadius": "8px",
    "shadow": true
  }
}
```

---

### M05 自定义主题

#### M05-01 主题编辑器布局

```
┌────────────────────────────────────────┐
│ 主题编辑器                              │
├───────────────┬────────────────────────┤
│               │                        │
│  设置面板      │     实时预览            │
│               │                        │
│  字体          │  (编辑 + 预览分屏)      │
│  颜色          │                        │
│  页面          │                        │
│  标题          │                        │
│  表格          │                        │
│  图片          │                        │
│  代码          │                        │
│               │                        │
└───────────────┴────────────────────────┘
```

所有修改实时预览，延迟 < 100ms。

#### M05-02 字体设置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 正文字体 | 系统已安装字体 + 推荐列表 | SF Pro Text / 苹方 |
| 标题字体 | 同上 | SF Pro Display / 苹方 |
| 代码字体 | 等宽字体列表 | JetBrains Mono / SF Mono |
| 字体大小 | 10–24px | 16px |
| 代码字体大小 | 10–20px | 14px |
| 行高 | 1.2–2.5 | 1.7 |
| 字间距 | -2px ~ 5px | 0px |
| 段间距 | 0.5em ~ 3em | 1em |
| 字重 | 300/400/500/600/700 | 400 |

推荐字体列表：苹方 (PingFang SC)、思源黑体、思源宋体、霞鹜文楷 (LXGW WenKai)、JetBrains Mono、Menlo、Monaco、SF Mono。

| 验收 | 字体列表实时预览；修改后预览即时刷新 |

#### M05-03 颜色设置

| 参数 | 说明 |
|---|---|
| 背景色 | 编辑区背景、预览区背景、代码块背景、引用块背景、表格行交替背景 |
| 正文颜色 | 普通文本颜色 |
| 标题颜色 | H1–H6 可分别设置颜色 |
| 链接颜色 | 普通链接、已访问链接、hover 链接 |
| 代码颜色 | 行内代码文字色、代码块文字色 |
| 强调色 | 加粗、斜体文本颜色 |
| 引用块颜色 | 引用标记线颜色、引用文字颜色 |
| 表格颜色 | 表头背景、边框颜色、交替行背景 |

| 操作方式 | macOS 原生 ColorPicker + HEX/RGB/HSL 手动输入 |
| 验收 | 颜色选择器弹出后实时预览；支持取色器吸管工具 |

#### M05-04 背景与页面设置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 背景类型 | 纯色 / 渐变 / 图片 | 纯色 |
| 渐变方向 | 水平 / 垂直 / 对角 / 径向 | 垂直 |
| 渐变色 | 起始色 + 结束色 | — |
| 背景图片 | 选择本地图片 / 内置纹理 | — |
| 背景图模式 | 填充 / 适应 / 拉伸 / 居中 | 填充 |
| 背景透明度 | 0%–100% | 100% |
| 背景模糊 | 0px–20px | 0px |
| 内容最大宽度 | 400–1200px | 800px |
| 左右边距 | 20–100px | 40px |
| 顶部边距 | 20–100px | 40px |
| 底部边距 | 20–100px | 40px |

#### M05-05 标题配置（H1–H6 分别设置）

每级标题支持以下参数：

| 参数 | H1 默认 | H2 默认 | H3 默认 | H4 默认 | H5 默认 | H6 默认 |
|---|---|---|---|---|---|---|
| 字号 | 28px | 24px | 20px | 18px | 16px | 14px |
| 颜色 | 比正文深 10% | 同上 | 同上 | 同上 | 同上 | 同上 |
| 字重 | 600 | 600 | 600 | 500 | 500 | 500 |
| 间距（上/下） | 1em | 1em | 0.8em | 0.8em | 0.6em | 0.6em |
| 下划线 | 关 | 关 | 关 | 关 | 关 | 关 |
| 分割线（下方） | 开 | 关 | 关 | 关 | 关 | 关 |

#### M05-06 代码块配置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 背景颜色 | 颜色选择器 | 深灰 (#1e1e1e) |
| 字体 | 等宽字体列表 | JetBrains Mono |
| 字体大小 | 10–20px | 14px |
| 圆角 | 0–12px | 6px |
| 边框 | 无 / 实线 / 虚线 + 颜色 + 宽度 | 无 |
| 代码高亮主题 | 见 M04-02 | 跟随编辑器 |
| 行号 | 开/关 | 关 |
| 内边距 | 8–32px | 16px |

#### M05-07 表格配置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 表格风格 | 简约线框 / 斑马纹 / 边框网格 / 无边框 | 斑马纹 |
| 表头背景色 | 颜色选择器 | 浅灰 |
| 表头字体颜色 | 颜色选择器 | 同标题 |
| 表头字重 | 500/600/700 | 600 |
| 边框颜色 | 颜色选择器 | 浅灰 |
| 边框宽度 | 0–3px | 1px |
| 斑马纹 | 开/关 + 颜色选择器 | 开 |
| 单元格内边距 | 4–20px | 8px |
| 表格宽度 | 自适应 / 100% 宽度 | 自适应 |
| 圆角 | 0–12px | 4px |

#### M05-08 图片配置

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| 最大宽度 | 50%–100% | 100% | 图片最大显示宽度占容器 |
| 默认居中 | 开/关 | 开 | 图片默认居中 |
| 圆角 | 0–20px | 4px | 图片圆角 |
| 阴影 | 无 / 浅 / 中 / 深 | 浅 | 投影效果 |
| 边框 | 无 / 颜色 + 宽度 | 无 | 图片边框 |
| 图片标题 | 开/关 | 开 | 显示 `![alt](url "title")` 中的 title |
| 间距 | 0–2em | 1em | 图片上下间距 |
| 点击放大 | 开/关 | 开 | 点击图片全屏查看 |
| 加载占位符 | 文字 / 模糊块 / 无 | 模糊块 | 图片加载中显示 |

#### M05-09 行高与间距

| 参数 | 范围 | 默认 |
|---|---|---|
| 正文行高 | 1.2–2.5 | 1.7 |
| 代码行高 | 1.2–2.0 | 1.5 |
| 段落间距 | 0.5–3em | 1em |
| 列表缩进 | 1–4em | 2em |
| 引用缩进 | 0.5–3em | 1em |
| 代码块内边距 | 8–32px | 16px |

#### M05-10 主题导入/导出

| 属性 | 说明 |
|---|---|
| 导出格式 | `.fmtheme`（JSON），包含全部主题参数 |
| 导入 | 支持拖拽 `.fmtheme` 文件到主题面板导入 |
| 校验 | 导入时校验 JSON 格式与版本兼容性；不兼容时提示并跳过不支持的参数 |
| 分享 | 导出的主题文件可分享给其他用户直接导入使用 |

---

### M06 Markdown 标准格式

基于 CommonMark 0.31 + GFM (GitHub Flavored Markdown) 规范。

| 编号 | 格式元素 | 语法示例 | 渲染效果 | 优先级 |
|---|---|---|---|---|
| F01 | 标题 | `# H1` ~ `###### H6` | 六级标题层级 | P0 |
| F02 | 段落 | 空行分隔 | 段落 | P0 |
| F03 | 加粗 | `**bold**` | **bold** | P0 |
| F04 | 斜体 | `*italic*` | *italic* | P0 |
| F05 | 删除线 | `~~del~~` | ~~del~~ | P0 |
| F06 | 行内代码 | `` `code` `` | `code` | P0 |
| F07 | 代码块 | ` ```lang ... ``` ` | 语法高亮代码块 | P0 |
| F08 | 有序列表 | `1. item` | 编号列表 | P0 |
| F09 | 无序列表 | `- item` | 项目符号列表 | P0 |
| F10 | 任务列表 | `- [x] done` | ☑ 复选框 | P0 |
| F11 | 引用块 | `> quote` | 引用样式块 | P0 |
| F12 | 水平分割线 | `---` | 水平线 | P0 |
| F13 | 链接 | `[text](url)` | 超链接 | P0 |
| F14 | 图片 | `![alt](url)` | 图片渲染 | P0 |

#### 渲染规格要点

- **标题**：H1 下方自动渲染水平分割线（可关闭）；自动生成锚点支持目录跳转和 URL hash 定位。
- **嵌套列表**：支持任意层级嵌套；Tab/Shift+Tab 调整缩进。
- **任务列表**：预览区中复选框可点击切换勾选状态，同步更新源码。
- **链接**：支持行内链接、引用式链接、自动链接（裸 URL）；内部链接（`.md` 文件）在编辑器内打开。

---

### M07 扩展格式

| 编号 | 功能 | 语法 | 渲染效果 | 优先级 |
|---|---|---|---|---|
| E01 | 数学公式（行内） | `$E=mc^2$` | KaTeX 行内渲染 | P0 |
| E02 | 数学公式（块级） | `$$...$$` | KaTeX 居中块级渲染 | P0 |
| E03 | Mermaid 流程图 | ` ```mermaid ... ``` ` | Mermaid 渲染 | P0 |
| E04 | 目录（TOC） | `[TOC]` 或 `[[toc]]` | 自动生成标题目录 | P0 |
| E05 | 脚注 | `[^1]` + `[^1]: text` | 脚注 + 底部注释 | P0 |
| E06 | 定义列表 | `Term\n: Definition` | 定义列表 | P1 |
| E07 | 表格扩展 | GFM 表格语法 | 支持列对齐 | P0 |
| E08 | Emoji | `:smile:` | 😀 Emoji 渲染 | P1 |
| E09 | Iconify 图标 | `:icon[mdi-home]:` | Iconify 图标渲染 | P1 |
| E10 | HTML 安全子集 | `<sub>` `<sup>` `<kbd>` `<u>` | 有限 HTML 渲染 | P0 |

#### E01-E02 数学公式
- 引擎：KaTeX（优先）；可选 MathJax 降级。
- 支持 LaTeX 语法：分数、根号、求和、积分、矩阵、上下标、希腊字母等。
- 支持 `\[...\]` 和 `\(...\)` 语法别名。
- 公式渲染失败时以红色错误提示显示原始 LaTeX 源码。
- 支持 `\color{red}{...}` 等颜色语法。

#### E03 Mermaid 图表
支持图表类型：

| 图表 | 语法关键字 |
|---|---|
| 流程图 | `flowchart` / `graph` |
| 时序图 | `sequenceDiagram` |
| 类图 | `classDiagram` |
| 状态图 | `stateDiagram` |
| ER 图 | `erDiagram` |
| 甘特图 | `gantt` |
| 饼图 | `pie` |
| 思维导图 | `mindmap` |
| 时间轴 | `timeline` |
| 用户旅程图 | `journey` |

- 图表渲染失败时显示错误信息和源码。
- 图表支持主题跟随（深色/浅色模式自动调整配色）。
- 图表支持点击放大查看。

#### E04 目录（TOC）
- 在文档中插入 `[TOC]` 自动生成当前文档的标题目录树。
- 目录项可点击跳转到对应标题位置。
- 目录自动响应标题变化（增删改后实时更新）。
- 支持设置目录最大层级（如仅显示 H1–H3）。
- 与大纲面板（M01-09）同步但独立于文档内容。

#### E05 脚注
- 支持 `[^id]` 定义脚注引用，`[^id]: 脚注内容` 定义脚注内容。
- 脚注引用渲染为上标数字链接，点击跳转到底部脚注区域。
- 脚注区域有返回箭头，编号自动排列。

#### E08-E09 Emoji 与 Iconify 图标
- **Emoji**：支持 `:shortcode:` 语法；内置 Emoji 速查面板（工具栏按钮唤出，可搜索后点击插入）；支持原生 Emoji 字符直接输入。
- **Iconify**：支持 `:icon[icon-set-name]:` 语法（如 `:icon[mdi-home]:`）；Iconify 覆盖 200,000+ 图标，开源免费；图标跟随文字颜色。

#### E10 HTML 安全子集
- 允许的安全标签：`<sub>`, `<sup>`, `<kbd>`, `<u>`, `<mark>`, `<details>`, `<summary>`, `<abbr>`, `<dl>`, `<dt>`, `<dd>`。
- **禁用**：`<script>`, `<iframe>`, `<object>`, `<embed>`, `<form>`, `<input>`, `<style>`。
- 允许的安全属性：`class`, `title`, `alt`, `colspan`, `rowspan`。

---

### M08 代码支持

#### M08-01 代码块语法高亮

| 属性 | 说明 |
|---|---|
| 引擎 | Shiki（VS Code 级别语法高亮） |
| 语言支持 | 200+ 编程语言 |
| 常用语言 | Java, Python, JavaScript, TypeScript, Go, Rust, C, C++, C#, Shell, SQL, HTML, CSS, JSON, YAML, XML, Dockerfile, Swift, Kotlin, PHP, Ruby, Scala, Lua, R, MATLAB |
| 语言标签 | 代码块右上角显示语言名 |
| 验收 | 高亮正确；未识别语言以纯文本显示 |

#### M08-02 代码块增强

| 功能 | 说明 |
|---|---|
| 复制按钮 | 代码块右上角「复制」按钮，一键复制代码内容 |
| 行号 | 可开关，显示在代码块左侧 |
| 自动换行 | 可开关，长行自动换行 |
| 代码折叠 | 行数超过阈值（默认 20 行）时自动折叠，点击展开 |
| 高亮指定行 | ` ```js {1,3-5} ` 语法高亮第 1、3-5 行 |
| 语言补全 | 输入 `` ``` `` 后自动弹出语言列表补全 |

#### M08-03 行内代码

| 属性 | 说明 |
|---|---|
| 样式 | 不同背景色 + 等宽字体 + 圆角 |
| 颜色 | 跟随主题（浅色：`#c7254e`，深色：`#e06c75`） |
| 快捷键 | `⌘E` 选中文本包裹为行内代码 |
| 验收 | 行内代码在段落中正确渲染 |

#### M08-04 代码高亮主题

见 M04-02，代码高亮主题独立于编辑器主题，可自由组合。

#### M08-05 代码块配置

见 M05-06，可在自定义主题中配置代码块的背景、字体、圆角、边框、行号等。

---

### M09 图片功能

#### M09-01 图片插入

| 方式 | 说明 |
|---|---|
| 本地拖拽 | 拖拽图片到编辑区，自动复制到 `assets/` 子目录，插入相对路径 |
| 文件选择 | 菜单「编辑 → 插入图片」弹出文件选择器 |
| 粘贴图片 | 截图后直接 `⌘V` 粘贴，自动保存为 PNG 并插入 |
| URL 图片 | 手动输入网络图片 URL |
| 路径补全 | 输入 `!` 后自动补全本地图片路径 |

#### M09-02 图片管理器

| 属性 | 说明 |
|---|---|
| 功能 | 提供图片资源管理面板，展示当前文档/工作区的所有图片 |
| 布局 | 网格缩略图展示，显示文件名和尺寸 |
| 操作 | 点击插入到文档；右键：删除、重命名、在 Finder 中显示 |
| 目录 | 默认 `assets/images/`，可配置 |
| 验收 | 图片列表加载 < 1s；删除图片时检查文档中是否有引用并提示 |

#### M09-03 图片编辑

| 功能 | 说明 |
|---|---|
| 调整大小 | 支持 `![alt](url =200x100)` 语法指定尺寸；或 `{width=80%}` 语法 |
| 对齐 | 左对齐 / 居中 / 右对齐 |
| 添加标题 | `![alt](url "caption")` 渲染为图片下方 caption |
| 点击放大 | 预览区点击图片全屏查看，支持缩放 |

#### M09-04 图片样式配置

见 M05-08，可在自定义主题中配置图片最大宽度、圆角、阴影、边框、间距等。

---

### M10 超链接与图标

#### M10-01 超链接

| 功能 | 说明 |
|---|---|
| 行内链接 | `[text](url)` |
| 引用式链接 | `[text][ref]` + `[ref]: url` |
| 自动链接 | 裸 URL 自动转为可点击链接 |
| URL Tooltip | 链接 hover 时显示 URL tooltip |
| 外部链接 | 点击时在系统默认浏览器打开 |
| 内部链接 | `.md` 文件链接在编辑器内打开（新 Tab） |
| 锚点链接 | `[章节](#heading)` 跳转到文档内标题 |
| 本地文件链接 | 点击本地文件路径在编辑器中打开 |
| URL 卡片 | 可选：将链接渲染为卡片预览（含标题、描述、缩略图） |

#### M10-02 Emoji 图标

见 E08。

#### M10-03 Iconify 图标

见 E09。推荐使用 Iconify 原因：图标库丰富（200,000+ 图标）、开源、支持大量图标集（Material Design Icons、Font Awesome、Tabler 等）。

---

### M11 文件管理与编码

#### M11-01 新建文件

| 属性 | 说明 |
|---|---|
| 操作 | `⌘N` / 菜单「文件 → 新建」/ 工具栏按钮 |
| 行为 | 创建空白文档（新 Tab），标题栏显示「未命名」；首次保存时弹出保存对话框 |
| 模板 | 支持选择空白模板、文档模板、技术文档模板、会议纪要模板 |
| 验收 | 新建后光标自动聚焦编辑区 |

#### M11-02 打开文件

| 属性 | 说明 |
|---|---|
| 操作 | `⌘O` / 菜单「文件 → 打开」/ 拖拽文件到窗口 |
| 格式 | `.md` `.markdown` `.mdx` `.txt` |
| 大文件 | 1MB+ 打开 < 2s |
| 记忆 | 打开后光标定位到上次编辑位置（可选） |

#### M11-03 保存与自动保存

| 属性 | 说明 |
|---|---|
| 手动保存 | `⌘S` 保存；`⌘⇧S` 另存为 |
| 修改标记 | 未保存时标题栏显示 `●`；保存后消失 |
| 自动保存模式 | 提供三种策略供用户选择 |

自动保存策略：

| 策略 | 说明 |
|---|---|
| 关闭 | 不自动保存，完全依赖手动 `⌘S` |
| 实时保存 | 停止输入 5s 后自动保存；窗口失焦时保存（默认） |
| 切换文件时保存 | 切换 Tab / 关闭 Tab 时自动保存 |

#### M11-04 未保存文件管理

关闭有未保存修改的 Tab 时弹出确认对话框：

```
┌──────────────────────────────────┐
│  此文件尚未保存                  │
│                                  │
│  PRD.md 有未保存的修改。          │
│                                  │
│  [取消]  [不保存]  [保存]        │
└──────────────────────────────────┘
```

- 「取消」：不关闭 Tab，返回编辑。
- 「不保存」：直接关闭，丢弃修改。
- 「保存」：保存后关闭。

#### M11-05 文件恢复

异常退出后重新打开时：

```
┌──────────────────────────────────┐
│  发现未恢复的文件                 │
│                                  │
│  PRD.md                          │
│  最后修改：2026-09-03 09:32      │
│                                  │
│  [恢复]  [丢弃]                  │
└──────────────────────────────────┘
```

- 草稿机制：每 30s 自动保存草稿到临时目录；正常退出时清理。
- 「恢复」：恢复草稿内容到编辑区。
- 「丢弃」：删除草稿。

#### M11-06 版本历史

| 属性 | 说明 |
|---|---|
| 功能 | 记录文件的修改历史快照，类似 macOS 版本浏览器 |
| 触发 | 每次保存时创建快照 |
| 展示 | 侧边面板显示时间线（如 `10:20 修改产品背景` / `10:05 新增用户需求` / `09:40 创建文档`） |
| 操作 | 点击某版本可预览内容；支持恢复到指定版本 |
| 限制 | 最多保留 100 个快照；超出后自动删除最旧的 |
| 存储 | 存储在 `~/Library/Application Support/FreeMarkdown/versions/` |
| 验收 | 恢复版本后文件内容正确；版本列表按时间倒序排列 |

#### M11-07 多编码支持

| 属性 | 说明 |
|---|---|
| 支持编码 | UTF-8、UTF-8 BOM、GBK、GB2312、GB18030、UTF-16 LE/BE、ASCII |
| 自动识别 | 打开文件时自动检测编码 |
| 编码转换 | 打开后可另存为不同编码 |
| 状态栏 | 状态栏显示当前编码 |
| 操作 | 菜单「文件 → 编码 → 重新以编码打开」弹出编码选择列表 |
| 验收 | GBK 文件正确打开无乱码；UTF-8 BOM 文件 BOM 标记正确处理（不显示为字符） |

#### M11-08 CJK 字体 fallback

| 属性 | 说明 |
|---|---|
| 功能 | 当主字体不包含某字符时，自动 fallback 到 CJK 字体 |
| fallback 链 | 主字体 → 苹方 (PingFang SC) → 思源黑体 → 系统默认 |
| 配置 | 设置中可自定义 CJK fallback 字体 |
| 验收 | 中英文混排正确显示；日文/韩文字符不显示为方块 |

#### M11-09 最近文件与工作区

| 属性 | 说明 |
|---|---|
| 最近文件 | 菜单「文件 → 最近打开」显示最近 20 个文件，按时间排序 |
| 首页 | 无文件打开时显示最近文件列表（文件名 + 相对时间） |
| 工作区 | 类似 VS Code，可打开一个文件夹作为工作区，侧边栏显示文件树 |
| 持久化 | 记录文件路径和最后打开时间 |
| 清理 | 文件不存在时标记为「缺失」，可手动移除 |

#### M11-10 文件树操作

| 操作 | 说明 |
|---|---|
| 新建文件 | 右键 → 新建 Markdown 文件，在当前目录创建 |
| 新建文件夹 | 右键 → 新建文件夹 |
| 重命名 | 右键 → 重命名 / 双击文件名；自动更新文档内对该文件的相对引用 |
| 删除 | 右键 → 删除，二次确认；移至废纸篓 |
| 在 Finder 中显示 | 右键 → 在 Finder 中显示 |
| 移动 | 拖拽文件到目标文件夹 |

---

### M12 搜索功能

#### M12-01 文档内搜索与替换

| 属性 | 说明 |
|---|---|
| 唤起 | `⌘F` 搜索；`⌘⌥F` 替换 |
| 功能 | 区分大小写、正则表达式、全词匹配 |
| 高亮 | 匹配项高亮；当前匹配项特殊高亮 |
| 统计 | 显示匹配数量和当前位置 |
| 验收 | 正则替换正确；大文件搜索 < 200ms |

#### M12-02 全局搜索

| 属性 | 说明 |
|---|---|
| 唤起 | `⌘⇧F` |
| 范围 | 搜索当前工作区/文件夹下所有 Markdown 文件 |
| 展示 | 结果列表显示文件名、匹配行号、匹配文本（高亮关键词） |
| 操作 | 点击结果项打开文件并定位到匹配行 |
| 过滤 | 支持按文件名过滤、排除特定目录 |
| 验收 | 500 个文件的全局搜索 < 3s |

---

### M13 导出与分享

#### M13-01 导出 PDF

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 导出 → PDF」/ `⌘⇧E` |
| 选项 | 页面大小（A4/A3/Letter）、页边距、是否包含目录、页码、页眉页脚 |
| 渲染 | 基于预览区渲染结果导出，保留主题样式 |
| 验收 | 导出的 PDF 与预览效果一致；公式和图表正确渲染 |

#### M13-02 导出 HTML

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 导出 → HTML」 |
| 选项 | 独立 HTML 文件（内联 CSS/JS）或带资源文件夹 |
| 内容 | 渲染后的 HTML + 主题样式 + 必要 JS（Mermaid/KaTeX 可选内联） |
| 验收 | 导出的 HTML 在浏览器中打开效果与编辑器预览一致 |

#### M13-03 导出 DOCX

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 导出 → DOCX」 |
| 引擎 | 通过 Pandoc 或自研转换器 |
| 保留 | 标题层级、表格、图片、列表、引用 |
| 限制 | Mermaid 图表导出为图片嵌入；数学公式导出为图片 |
| 验收 | 导出的 DOCX 在 Word/Pages 中打开排版正确 |
| 优先级 | P1（V2.0 实现） |

#### M13-04 导出图片

| 属性 | 说明 |
|---|---|
| 操作 | 右键预览区 → 导出为图片 |
| 格式 | PNG / JPEG |
| 选项 | 背景透明（PNG）、分辨率倍数（1x/2x/3x）、裁剪空白边距 |
| 验收 | Retina 屏下 2x/3x 无锯齿 |

#### M13-05 复制为 HTML

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「编辑 → 复制为 HTML」/ `⌘⇧C` |
| 行为 | 将预览区渲染后的 HTML（含内联样式）复制到剪贴板 |
| 验收 | 粘贴到富文本编辑器（邮件、Pages）保留格式 |

#### M13-06 复制为纯文本

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「编辑 → 复制为纯文本」/ `⌘⌥C` |
| 行为 | 将 Markdown 去除语法标记后复制为纯文本 |
| 验收 | 粘贴到纯文本编辑器无 Markdown 符号残留 |

#### M13-07 打印

| 属性 | 说明 |
|---|---|
| 操作 | `⌘P` / 菜单「文件 → 打印」 |
| 行为 | 调用 macOS 打印对话框，打印预览区渲染结果 |
| 验收 | 打印预览正确分页；公式/图表在打印中可读 |

---

### M14 写作辅助

#### M14-01 基础统计

| 属性 | 说明 |
|---|---|
| 显示内容 | 字数、字符数、段落数、阅读时间、写作时间 |
| 位置 | 状态栏 / 专注模式下的信息浮层 |
| 实时 | 输入时实时更新 |
| 验收 | 中英文混合正确统计 |

#### M14-02 专注模式

| 属性 | 说明 |
|---|---|
| 功能 | 隐藏侧边栏、工具栏，全屏编辑，自动弱化非当前段落 |
| 弱化效果 | 非当前段落透明度降至 30% |
| 背景 | 背景色可调 |
| 退出 | `Esc` |
| 验收 | 仅当前段落高亮；输入时焦点正确 |

#### M14-03 番茄钟

| 属性 | 说明 |
|---|---|
| 功能 | 内置番茄钟写作计时器 |
| 默认 | 25 分钟工作 + 5 分钟休息 |
| 显示 | 状态栏或浮层显示倒计时 |
| 完成提醒 | 番茄钟结束时系统通知 |
| 写作统计 | 记录每个番茄钟内的写作字数 |
| 配置 | 可设置工作/休息时长 |
| 验收 | 倒计时准确；通知正常弹出 |

#### M14-04 写作目标

| 属性 | 说明 |
|---|---|
| 功能 | 用户可设置每日写作字数目标（如 3000 字） |
| 展示 | 状态栏或专注模式下显示「当前字数 / 今日目标」进度条 |
| 统计 | 记录每日写作字数历史 |
| 提醒 | 达成目标时弹出祝贺通知 |
| 验收 | 字数实时更新；进度条正确显示 |

---

### M15 设置与快捷键

#### M15-01 通用设置

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 启动时打开 | 上次文件 / 空白文档 / 最近文件列表 | 上次文件 | — |
| 默认编码 | UTF-8 / GBK / 其他 | UTF-8 | — |
| 默认换行符 | LF / CRLF | LF | — |
| 检查更新 | 开/关 + 频率 | 开，每日 | 应用更新检查 |
| 语言 | 简体中文 / English | 跟随系统 | 界面语言 |
| CJK fallback 字体 | 字体选择器 | 苹方 | CJK 字体回退 |

#### M15-02 编辑器设置

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 显示行号 | 开/关 | 开 | 编辑区左侧行号 |
| 自动换行 | 开/关 | 开 | 长行自动换行 |
| Tab 大小 | 2/4/8 | 2 | 缩进宽度 |
| 空格替代 Tab | 开/关 | 开 | — |
| 代码折叠 | 开/关 | 开 | — |
| 显示空白字符 | 开/关 | 关 | 空格/Tab 可视化 |
| 光标样式 | 竖线 / 下划线 / 块 | 竖线 | — |
| 光标闪烁 | 开/关 | 开 | — |
| 编辑区字体大小 | 10–24px | 14px | 源码编辑区字体大小 |

#### M15-03 预览与渲染设置

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 预览实时刷新 | 开/关 | 开 | — |
| 预览区字体大小 | 10–24px | 16px | 预览渲染区字体大小 |
| 代码块行号 | 开/关 | 关 | 预览区代码块是否显示行号 |
| 代码块折叠 | 开/关 | 开 | — |
| 图片懒加载 | 开/关 | 开 | 滚动到可视区域才加载图片 |
| 数学公式引擎 | KaTeX / MathJax | KaTeX | — |
| Mermaid 主题跟随 | 开/关 | 开 | 图表主题跟随编辑器主题 |
| 脚注渲染 | 开/关 | 开 | — |
| 任务列表可交互 | 开/关 | 开 | 预览区可点击复选框 |
| Emoji 渲染 | 开/关 | 开 | `:shortcode:` → Emoji |
| Iconify 渲染 | 开/关 | 开 | `:icon[...]:` → 图标 |
| 自动链接 | 开/关 | 开 | 裸 URL 自动转为链接 |
| 换行渲染 | soft / hard | soft | soft=单换行不渲染为段落 |

#### M15-04 快捷键自定义

| 属性 | 说明 |
|---|---|
| 功能 | 设置面板中可查看和修改所有快捷键 |
| 操作 | 点击快捷键项 → 按下新组合键 → 确认 |
| 冲突检测 | 新设置与系统/应用已有快捷键冲突时提示 |
| 重置 | 每项可单独重置为默认；可一键全部重置 |
| 导入/导出 | 支持快捷键配置导入导出（JSON） |

#### M15-05 快捷键总表

**全局快捷键**

| 快捷键 | 功能 |
|---|---|
| `⌘N` | 新建文件（新 Tab） |
| `⌘O` | 打开文件 |
| `⌘S` | 保存 |
| `⌘⇧S` | 另存为 |
| `⌘W` | 关闭 Tab/窗口 |
| `⇧⌘T` | 重新打开关闭的 Tab |
| `⌘Q` | 退出 |
| `⌘P` | 打印 |
| `⌘F` | 搜索 |
| `⌘⌥F` | 替换 |
| `⌘⇧F` | 全局搜索 |
| `⌘Z` / `⌘⇧Z` | 撤销/重做 |
| `⌘,` | 打开设置 |

**视图快捷键**

| 快捷键 | 功能 |
|---|---|
| `⌘1` | 仅编辑模式 |
| `⌘2` | 仅预览模式 |
| `⌘3` | 分屏对比模式 |
| `⌘4` | 所见即所得模式 |
| `⌘\` | 切换侧边栏 |
| `⌘⇧T` | 切换主题 |
| `⌘+` / `⌘-` | 放大/缩小字体 |
| `⌘0` | 重置字体大小 |
| `⌘⇧F` | 全屏 / 沉浸模式 |
| `⌘⌥→` / `⌘⌥←` | 下一个/上一个 Tab |

**格式化快捷键**

| 快捷键 | 功能 |
|---|---|
| `⌘B` | 加粗 |
| `⌘I` | 斜体 |
| `⌘E` | 行内代码 |
| `⌘K` | 插入链接 |
| `⌘T` | 插入表格 |
| `⌘⇧X` | 删除线 |
| `⌘⇧L` | 任务列表 |
| `⌘⇧C` | 复制为 HTML |
| `⌘⌥C` | 复制为纯文本 |
| `⌘⇧E` | 导出 |
| `⌘⇧]` / `⌘⇧[` | 标题层级升降级 |
| `Tab` / `⇧Tab` | 列表缩进/反缩进 |

---

### M16 macOS 原生集成

#### M16-01 系统外观集成

| 属性 | 说明 |
|---|---|
| Dark/Light Mode | 跟随系统深色/浅色模式自动切换主题 |
| 系统字体 | 使用 macOS 系统字体（苹方 / SF Pro） |
| 全屏 | 支持 macOS 原生全屏 |
| Stage Manager | 兼容 macOS Stage Manager |
| 多显示器 | 支持多显示器，窗口可拖拽到不同屏幕 |
| 减少动态效果 | 跟随系统「减少动态效果」设置关闭动画 |
| 增加对比度 | 跟随系统「增加对比度」设置 |

#### M16-02 文件系统集成

| 属性 | 说明 |
|---|---|
| Finder 拖拽 | 拖拽文件从 Finder 到编辑器窗口 |
| Quick Look | 支持 macOS Quick Look 预览 `.md` 文件（通过扩展） |
| 文件关联 | `.md` `.markdown` 文件可关联到本应用打开 |
| iCloud Drive | 支持打开和保存 iCloud Drive 中的文件 |
| 最近文件 | macOS 系统菜单栏「最近打开」集成 |

#### M16-03 剪贴板集成

| 属性 | 说明 |
|---|---|
| 复制 | `⌘C` 复制选中文本（Markdown 源码） |
| 粘贴 | `⌘V` 粘贴文本；粘贴图片自动保存为文件 |
| 复制为 HTML | `⌘⇧C` 复制渲染后的 HTML |
| 复制为纯文本 | `⌘⌥C` 复制去除标记的纯文本 |
| 富文本粘贴 | 粘贴富文本时自动转为 Markdown 语法 |

#### M16-04 可访问性

| 属性 | 说明 |
|---|---|
| VoiceOver | 支持 macOS VoiceOver 屏幕阅读器 |
| Tab 导航 | 所有交互元素支持 Tab 键导航 |
| 对比度 | 颜色对比度满足 WCAG 2.1 AA 标准（正文与背景对比度 ≥ 4.5:1） |
| 减少动态效果 | 跟随系统设置关闭动画过渡 |
| 增加对比度 | 跟随系统设置增加 UI 对比度 |

---

## 5. 非功能需求

### 5.1 性能

| 指标 | 目标 |
|---|---|
| 冷启动时间 | < 1.5s |
| 普通文档打开 | < 500ms |
| 打开 1MB Markdown 文件 | < 2s |
| 10MB Markdown 文件 | 正常编辑 |
| 10,000 行文档 | 流畅编辑 |
| 预览渲染延迟 | < 100ms（输入到预览更新） |
| 内存占用 | < 300MB（单文档） |
| 搜索响应 | < 200ms（10万行文档） |
| 主题切换 | < 300ms |
| Tab 切换 | < 50ms |

### 5.2 兼容性

| 平台 | 版本 |
|---|---|
| macOS | 13 Ventura 及以上 |
| 架构 | Apple Silicon (arm64) 优先 + Intel (x86_64) 兼容 |

### 5.3 安全性

- 不上传用户文档到任何服务器（纯本地应用）。
- 打开外部链接前可设置二次确认。
- 文件写入操作仅在用户明确操作下触发（保存/导出）。
- 不执行 Markdown 中的嵌入式脚本（禁用 `<script>`, `<iframe>` 等标签）。
- HTML 内联渲染白名单（见 E10）。

### 5.4 本地化

- 界面语言：简体中文、English（首版），预留多语言框架。
- 日期/时间格式：跟随系统区域设置。
- 输入法：支持 macOS 所有输入法，中文输入法下不触发 Markdown 快捷键。
- CJK 字体 fallback（见 M11-08）。

### 5.5 持久化与配置

| 数据类型 | 存储位置 | 格式 |
|---|---|---|
| 应用设置 | `~/Library/Application Support/FreeMarkdown/` | `settings.json` |
| 自定义主题 | 同上 `/themes/` | `*.fmtheme` (JSON) |
| 快捷键配置 | 同上 | `keybindings.json` |
| 草稿/自动恢复 | 同上 `/drafts/` | 临时 `.md` 文件 |
| 版本历史 | 同上 `/versions/` | 快照文件 |
| 最近文件列表 | 同上 | `recent.json` |

---

## 6. 技术架构

### 6.1 技术方案对比

| 方案 | 技术栈 | 推荐指数 | 优点 | 缺点 |
|---|---|---|---|---|
| **A: 原生 macOS** | Swift + SwiftUI + AppKit | ★★★★★ | 性能最好，macOS 原生体验最佳，内存低，Apple Silicon 原生支持，系统集成强 | 仅 macOS，开发学习曲线 |
| **B: Tauri** | Rust + Tauri 2.0 + React | ★★★★☆ | 性能优秀，包体积小，未来跨平台，前端生态丰富 | macOS 原生体验不如方案 A |
| **C: Electron** | Electron + React/Vue | ★★★☆☆ | 开发快，编辑器生态成熟 | 内存高，包体大，不符合轻量定位 |

### 6.2 推荐架构：原生 macOS（方案 A）

```
┌──────────────────────────────────────┐
│           SwiftUI UI 层               │
│  Tab栏 │ 工具栏 │ 侧边栏 │ 设置面板    │
├──────────────────────────────────────┤
│         编辑器引擎                    │
│  NSTextView / CodeMirror 6 (WebView) │
│  语法高亮 │ 快捷键 │ 自动补全 │ 折叠    │
├──────────────────────────────────────┤
│       Markdown 解析引擎               │
│  markdown-it / CommonMark             │
│  解析 → AST → 插件处理 → HTML         │
├──────────────────────────────────────┤
│         渲染引擎                       │
│  KaTeX (公式) │ Mermaid (图表)        │
│  Shiki (代码高亮) │ Iconify (图标)    │
├──────────────────────────────────────┤
│         主题引擎 (CSS Variables)       │
│  预置主题 │ 自定义主题 │ 导入/导出      │
├──────────────────────────────────────┤
│       原生能力层 (Rust/Swift)         │
│  文件IO │ 窗口管理 │ 剪贴板 │ 编码检测 │
│  PDF导出 │ 系统菜单 │ FileWatcher     │
└──────────────────────────────────────┘
```

### 6.3 核心技术选型

| 功能 | 推荐技术 |
|---|---|
| 编辑器内核 | CodeMirror 6（通过 WebView）或 NSTextView |
| Markdown 解析 | markdown-it |
| 数学公式 | KaTeX |
| Mermaid 图表 | Mermaid.js |
| 代码高亮 | Shiki |
| 图标 | Iconify |
| PDF 导出 | WKWebView Print |
| 数据库 | SQLite（文档元数据） |
| 文件监听 | macOS FileWatcher (DispatchSource) |
| 编码检测 | ICU / chardet |

---

## 7. 竞品差异化分析

| 产品 | 优势 | 不足 | 本产品差异化 |
|---|---|---|---|
| Typora | 易用、所见即所得成熟 | 收费、定制能力有限 | 免费且可深度定制 |
| Obsidian | 强大、插件生态 | 更像知识库，非纯编辑器 | 纯编辑器定位，开箱即用 |
| MarkText | 免费、开源 | UI 与 macOS 原生体验一般 | 原生 macOS 体验更优 |
| VS Code | 强大、扩展性强 | 太重，非专注编辑器 | 轻量专注，Markdown 体验更精 |
| MacDown | 免费、开源 | 功能较传统，UI 老旧 | 现代 UI，主题/多Tab/多编码等新特性 |

### 核心竞争力

1. **多编码支持**——GBK/GB18030/UTF-16 等 + 自动识别 + 转换，国内用户痛点。
2. **多 Tab + 多窗口**——类 VS Code 的文件管理体验，但更轻量。
3. **CJK 排版优化**——中英文混排、CJK 字体 fallback、中文输入法兼容。
4. **深度主题定制**——字体/颜色/背景/表格/图片/代码全维度可定制 + 主题导入导出分享。
5. **免费开源**——核心功能永久免费。

---

## 8. 版本规划

### V1.0（MVP）

**目标：核心编辑器可用，覆盖主要需求**

- 4 种编辑模式（纯源码 / 双栏预览 / 所见即所得 / 沉浸写作）
- 多 Tab + 多窗口
- CommonMark + GFM 全格式
- 数学公式（KaTeX）
- Mermaid 图表
- 6 套预置主题 + 代码高亮主题
- 自定义主题编辑器（字体/颜色/背景/表格/图片/代码）
- 文件管理（打开/保存/自动保存/最近文件/文件恢复）
- 多编码支持（UTF-8/GBK/GB18030 等）
- 导出 PDF/HTML
- 搜索替换（文档内）
- macOS 原生集成

### V1.5

- 表格可视化编辑（增删行列/拖拽列宽/对齐）
- 图片拖拽 + 图片资源管理器
- 文档大纲 + TOC
- 全局搜索
- 主题导入/导出
- 版本历史
- 番茄钟 + 写作目标
- Tab 分屏对比

### V2.0

- DOCX 导出
- MDX 支持
- 插件系统（扩展 API）
- Git 集成（查看 diff、提交）
- 多文件工作区增强
- Iconify 图标支持
- 主题市场（社区分享）

### V3.0（远期 — AI 能力）

架构提前预留 AI 能力接入点：

| 功能 | 说明 |
|---|---|
| AI 写作辅助 | AI 改写、AI 总结、AI 扩写、AI 翻译、AI 润色 |
| AI Markdown 助手 | 自然语言生成 Markdown（如「生成一个用户登录流程图」→ Mermaid 代码） |
| 本地 AI | 支持 Ollama / LM Studio / OpenAI Compatible API |
| 定位 | 完全本地 Markdown + 本地 AI，不强制云端 |

---

## 9. 验收清单

### 9.1 界面与布局

- [ ] macOS 原生窗口体验，红绿灯按钮正常
- [ ] 三种布局模式（仅编辑/仅预览/分屏）切换正常
- [ ] 分屏双向滚动同步
- [ ] 侧边栏文件树正常（新建/重命名/删除/Finder 显示/复制路径）
- [ ] 工具栏/状态栏显示正常
- [ ] 大纲导航面板正常
- [ ] 全屏/沉浸模式正常

### 9.2 编辑模式

- [ ] 纯源码模式语法高亮正确
- [ ] 双栏预览 < 100ms 延迟
- [ ] 所见即所得模式渲染正确，切回源码保留语法
- [ ] 沉浸写作模式非当前段落弱化

### 9.3 多 Tab 与多窗口

- [ ] 多 Tab 新建/关闭/切换正常
- [ ] Tab 拖拽排序正常
- [ ] Tab 右键菜单完整（关闭/固定/复制路径/Finder/重新打开）
- [ ] 未保存文件关闭时弹出确认对话框
- [ ] 多窗口独立运行正常

### 9.4 主题

- [ ] 6 个预置主题可正常切换
- [ ] 代码高亮主题独立切换
- [ ] 跟随系统外观模式正常
- [ ] 主题切换有过渡动画
- [ ] 主题导入/导出 `.fmtheme` 正常

### 9.5 自定义主题

- [ ] 字体/颜色/背景/标题/代码/表格/图片设置全部生效
- [ ] 主题编辑器实时预览 < 100ms
- [ ] 自定义主题可保存/编辑/删除/恢复默认

### 9.6 Markdown 格式

- [ ] CommonMark 14 种标准格式全部正确渲染
- [ ] 代码块 200+ 语言高亮、复制按钮、行号、折叠、高亮指定行
- [ ] 任务列表可交互
- [ ] 链接内部跳转和外部浏览器打开正常
- [ ] 图片本地/网络/拖拽/粘贴均正常

### 9.7 扩展格式

- [ ] 行内/块级公式正确渲染
- [ ] Mermaid 全图表类型正确渲染
- [ ] TOC 自动生成且可跳转
- [ ] 脚注正确渲染且可跳转
- [ ] Emoji `:shortcode:` 正确渲染
- [ ] HTML 安全子集正确渲染，危险标签被过滤

### 9.8 文件管理与编码

- [ ] 新建/打开/保存/另存为正常
- [ ] 自动保存三种策略可选
- [ ] 异常退出后文件恢复正常
- [ ] 版本历史快照正常
- [ ] GBK 文件正确打开无乱码
- [ ] 编码转换正常
- [ ] CJK 字体 fallback 正确

### 9.9 搜索

- [ ] 文档内搜索（正则/大小写/全词）正常
- [ ] 全局搜索（跨文件）正常

### 9.10 导出

- [ ] 导出 PDF（含目录/页码/页眉页脚）正确
- [ ] 导出 HTML 在浏览器中效果一致
- [ ] 导出图片清晰
- [ ] 复制为 HTML 保留格式
- [ ] 复制为纯文本无残留

### 9.11 写作辅助

- [ ] 字数/字符/段落统计正确
- [ ] 番茄钟倒计时准确
- [ ] 写作目标进度条正确

### 9.12 macOS 原生集成

- [ ] Dark/Light 模式跟随系统切换
- [ ] Finder 拖拽正常
- [ ] iCloud Drive 文件正常打开/保存
- [ ] VoiceOver 可读编辑区和预览区
- [ ] 多显示器正常

### 9.13 非功能

- [ ] 冷启动 < 1.5s
- [ ] 打开 1MB 文件 < 2s
- [ ] 内存占用 < 300MB
- [ ] 颜色对比度满足 WCAG AA

---

## 10. 变更记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| V1.0 | 2026-09-03 | 初版需求说明书，10 个功能模块 |
| V2.0 | 2026-09-03 | 融合参考文档，扩展至 16 个功能模块；新增 4 种编辑模式、多 Tab/多窗口、多编码、CJK fallback、写作辅助、版本历史、Iconify 图标、学术主题、代码高亮主题独立、macOS 原生集成、全局搜索、工作区、竞品分析、AI 路线图；技术架构新增 Swift 原生方案对比 |
