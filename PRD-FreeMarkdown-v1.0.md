# FreeMarkdown — macOS Markdown 编辑器产品需求说明书

| 字段 | 值 |
|---|---|
| 文档版本 | V1.0 |
| 创建日期 | 2026-09-03 |
| 产品名称 | FreeMarkdown（暂定） |
| 目标平台 | macOS 13 Ventura 及以上，Apple Silicon / Intel |
| 授权模式 | 完全免费，开源（MIT 或同类许可证） |
| 文档状态 | 初稿 |

---

## 1. 产品概述

### 1.1 产品定位

一款原生 macOS 系统的免费 Markdown 编辑器，面向程序员、技术写作者、学生和办公人士。核心价值主张：

1. **开箱即用的美观界面**——不依赖复杂配置即获得优秀视觉体验。
2. **多场景主题体系**——预置办公、夜间、程序员阅读、写作等场景主题，一键切换。
3. **深度自定义**——用户可精细调整字体、颜色、背景、行高、字号、字色、表格样式、图片尺寸等。
4. **全格式 Markdown 支持**——覆盖 CommonMark / GFM 全部标准元素，并扩展支持数学公式、流程图、Mermaid 图表、目录、脚注、任务列表等。

### 1.2 目标用户

| 用户角色 | 核心诉求 |
|---|---|
| 程序员 | 代码高亮、暗色主题、快速导出、Git 友好 |
| 技术写作者 | 长文排版、目录自动生成、导出 PDF/HTML |
| 学生 / 研究人员 | 数学公式（LaTeX）、流程图、思维导图 |
| 办公人士 | 表格美化、图片缩放、导出 Word/PDF、所见即所得 |

### 1.3 非目标（本期不覆盖）

- 不做云端同步 / 在线协作（后续版本规划）。
- 不做移动端（iOS/iPadOS）版本。
- 不做插件市场（保留扩展 API 但不运营市场）。

---

## 2. 技术架构概要

### 2.1 技术选型建议

| 层级 | 技术方案 | 说明 |
|---|---|---|
| 应用框架 | Tauri 2.0 | Rust 后端 + Web 前端，轻量原生，包体小，macOS 原生窗口 |
| 前端框架 | React 19 + TypeScript | 组件化开发，生态成熟 |
| 编辑器内核 | CodeMirror 6 | 高性能 Markdown 源码编辑，插件生态丰富 |
| 预览引擎 | markdown-it + 扩展插件链 | 支持 CommonMark/GFM，插件化扩展 |
| 代码高亮 | Shiki | VS Code 级别语法高亮，支持主题 |
| 数学公式 | KaTeX | 快速、轻量的 LaTeX 公式渲染 |
| 图表渲染 | Mermaid.js | 流程图、时序图、甘特图等 |
| 状态管理 | Zustand | 轻量全局状态 |
| 持久化 | 本地 SQLite (可选) + JSON 配置文件 | 文档元数据 + 主题/设置持久化 |

### 2.2 架构分层

```
┌───────────────────────────────────────────────┐
│                   UI 层 (React)                │
│  编辑区 │ 预览区 │ 工具栏 │ 侧边栏 │ 设置面板    │
├───────────────────────────────────────────────┤
│              编辑器引擎 (CodeMirror 6)          │
│  语法高亮 │ 快捷键 │ 自动补全 │ 折叠 │ 搜索      │
├───────────────────────────────────────────────┤
│           Markdown 渲染管线 (markdown-it)        │
│  解析 → AST → 插件处理 → HTML → 增强(公式/图表)  │
├───────────────────────────────────────────────┤
│              主题引擎 (CSS Variables)            │
│  预置主题 │ 自定义主题 │ 主题导入/导出             │
├───────────────────────────────────────────────┤
│          原生能力层 (Rust / Tauri)              │
│  文件IO │ 窗口管理 │ 剪贴板 │ 导出PDF │ 系统菜单  │
└───────────────────────────────────────────────┘
```

---

## 3. 功能模块总览

| 编号 | 功能模块 | 功能点数 | 优先级 |
|---|---|---|---|
| M01 | 界面与布局 | 8 | P0 |
| M02 | 主题体系 | 6 | P0 |
| M03 | 自定义主题 | 10 | P0 |
| M04 | Markdown 标准格式支持 | 14 | P0 |
| M05 | 扩展格式支持（公式/图表/目录等） | 8 | P0 |
| M06 | 文件管理 | 7 | P0 |
| M07 | 导出与分享 | 6 | P1 |
| M08 | 编辑增强 | 8 | P1 |
| M09 | 设置与偏好 | 5 | P1 |
| M10 | 快捷键体系 | 3 | P1 |

---

## 4. 详细需求

## M01 界面与布局

### M01-01 窗口与标题栏

| 属性 | 说明 |
|---|---|
| 操作/方法 | macOS 原生窗口，支持左上角红绿灯按钮（关闭/最小化/全屏） |
| 规则 | 标题栏显示当前文件名 + 是否已修改标记（`●`）；未保存文件显示 `未命名` |
| 快捷键 | `⌘W` 关闭窗口；`⌘M` 最小化 |
| 验收 | 窗口拖拽正常；全屏后退出恢复原大小；多窗口独立 |

### M01-02 编辑/预览布局模式

| 模式 | 说明 | 触发方式 |
|---|---|---|
| 仅编辑 | 全屏源码编辑 | 工具栏按钮 / `⌘1` |
| 仅预览 | 全屏渲染预览 | 工具栏按钮 / `⌘2` |
| 分屏对比 | 左编辑右预览，实时同步滚动 | 工具栏按钮 / `⌘3`（默认） |
| 分屏（预览在左） | 可交换左右位置 | 设置项 |

- 分屏模式下拖动中间分隔条可调整编辑区与预览区的宽度比例（3:7 ~ 7:3）。
- 分隔条位置记忆持久化，下次打开恢复。
- 编辑区与预览区双向滚动同步（编辑滚动→预览跟随；预览滚动→编辑跟随）。

### M01-03 侧边栏（文件树）

| 属性 | 说明 |
|---|---|
| 功能 | 展示已打开文件夹的文件树，支持 Markdown 文件快速切换 |
| 操作 | 点击文件名打开；右键菜单：新建、重命名、删除、在 Finder 中显示 |
| 收起/展开 | 侧边栏可收起，`⌘\` 切换显示 |
| 搜索 | 侧边栏顶部搜索框，按文件名实时过滤 |
| 验收 | 大目录（500+ 文件）加载 < 1s；树形展开/折叠有动画 |

### M01-04 工具栏

| 属性 | 说明 |
|---|---|
| 位置 | 窗口顶部，标题栏下方 |
| 内容 | 布局切换、主题切换、快速格式按钮（加粗/斜体/链接/图片/表格/代码块）、导出、设置 |
| 可定制 | 用户可隐藏/显示工具栏，设置中配置按钮可见性 |
| 验收 | 工具栏按钮有 hover/active 状态；暗色主题下图标清晰 |

### M01-05 状态栏

| 属性 | 说明 |
|---|---|
| 位置 | 窗口底部 |
| 显示内容 | 字符数、词数、行数、列数、当前光标位置（行:列）、编码格式、文件路径 |
| 切换 | 设置中可自定义显示项 |
| 验收 | 实时更新（输入时同步）；数字格式化（千分位） |

### M01-06 面包屑导航（大纲）

| 属性 | 说明 |
|---|---|
| 功能 | 编辑区顶部显示当前光标所在标题层级面包屑 |
| 操作 | 点击某层级标题可快速跳转到该标题位置 |
| 验收 | 标题层级正确；跳转后光标定位准确 |

### M01-07 字数统计与阅读时间

| 属性 | 说明 |
|---|---|
| 功能 | 状态栏或工具栏显示：中文字数、英文词数、阅读预估时间（按 300字/分钟） |
| 验收 | 中英文混合正确统计；阅读时间取整显示 |

### M01-08 拖拽支持

| 属性 | 说明 |
|---|---|
| 拖拽文件 | 拖拽 `.md` 文件到窗口直接打开 |
| 拖拽图片 | 拖拽图片到编辑区自动插入 Markdown 图片语法，图片复制到文档同目录 `assets/` 子目录 |
| 拖拽文本 | 拖拽纯文本以引用方式插入 |
| 验收 | 拖拽有视觉反馈（高亮边框）；图片自动复制到 assets 并生成相对路径 |

---

## M02 主题体系

### M02-01 预置主题列表

| 编号 | 主题名称 | 适用场景 | 预览色 |
|---|---|---|---|
| T01 | **办公白**（Office Light） | 日常办公、打印预览 | 白底黑字，浅灰边框，蓝色链接 |
| T02 | **夜间深色**（Dark Night） | 夜间、低光环境 | 深灰底浅字，低对比度护眼 |
| T03 | **程序员阅读**（Code Reader） | 代码/技术文档阅读 | 类似 VS Code 暗色，等宽字体为主 |
| T04 | **写作专注**（Focus Writer） | 长文写作、沉浸创作 | 米黄底，衬线字体，宽行距，居中窄栏 |
| T05 | **护眼绿**（Eye Care Green） | 长时间阅读护眼 | 豆沙绿底 (#C7EDCC)，深绿字 |
| T06 | **极简灰**（Minimal Gray） | 极简风格 | 浅灰底，灰色字，无多余装饰 |
| T07 | **羊皮纸**（Parchment） | 文学/古风写作 | 淡棕底，衬线字体 |

### M02-02 主题切换

| 属性 | 说明 |
|---|---|
| 操作 | 工具栏主题下拉菜单 / 设置面板 / 快捷键 `⌘⇧T` |
| 预览 | 主题选择面板实时预览效果（编辑区内容不变，仅样式切换） |
| 持久化 | 切换后自动保存为默认主题 |
| 动画 | 切换时有 200ms 过渡动画，避免突兀闪烁 |

### M02-03 跟随系统外观

| 属性 | 说明 |
|---|---|
| 功能 | 可设置「跟随系统外观」，macOS 切换深色/浅色模式时编辑器自动切换对应主题 |
| 规则 | 用户分别配置「浅色模式主题」和「深色模式主题」 |
| 验收 | 系统切换外观时编辑器 300ms 内切换主题 |

### M02-04 主题管理

| 属性 | 说明 |
|---|---|
| 新建 | 从现有主题「另存为」创建自定义主题副本 |
| 编辑 | 跳转至自定义主题编辑器（见 M03） |
| 删除 | 可删除自定义主题；预置主题不可删除 |
| 导入/导出 | 支持以 `.fmtheme`（JSON 格式）导入导出主题，方便分享 |

### M02-05 主题应用范围

| 范围 | 说明 |
|---|---|
| 编辑区 | 语法高亮配色、背景、字体 |
| 预览区 | 渲染后的 HTML 样式（标题/段落/列表/代码块/表格等） |
| UI 界面 | 工具栏、侧边栏、状态栏、面板 |

### M02-06 主题预览卡片

| 属性 | 说明 |
|---|---|
| 功能 | 主题选择面板中每个主题展示缩略预览卡片 |
| 内容 | 卡片展示标题、正文、代码块、表格的渲染效果色样 |
| 验收 | 卡片尺寸统一（约 200×120px）；hover 时放大显示更多细节 |

---

## M03 自定义主题

### M03-01 自定义主题编辑器入口

| 属性 | 说明 |
|---|---|
| 入口 | 设置 → 主题 → 选中自定义主题 → 编辑；或主题面板「创建新主题」 |
| 布局 | 左侧实时预览（编辑+预览分屏），右侧分类设置面板 |
| 验收 | 修改任意参数后左侧预览 < 100ms 内更新 |

### M03-02 字体设置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 正文字体 | 系统已安装字体列表 + 常用推荐 | SF Pro Text |
| 标题字体 | 同上 | SF Pro Display |
| 代码字体 | 等宽字体列表 | JetBrains Mono / SF Mono |
| 字体大小 | 10–24px，步进 1px | 16px |
| 代码字体大小 | 10–20px | 14px |
| 行高 | 1.2–2.5，步进 0.1 | 1.7 |
| 字间距 | -2px ~ 5px | 0px |
| 段间距 | 0.5em ~ 3em | 1em |
| 字重 | 300/400/500/600/700 | 400 |

| 验收 | 字体列表实时预览；修改后预览即时刷新 |

### M03-03 颜色设置

| 参数 | 说明 |
|---|---|
| 背景色 | 编辑区背景、预览区背景、代码块背景、引用块背景、表格行交替背景 |
| 正文颜色 | 普通文本颜色 |
| 标题颜色 | H1–H6 可分别设置颜色 |
| 链接颜色 | 普通链接、已访问链接、hover 链接 |
| 代码颜色 | 行内代码文字色、代码块文字色 |
| 强调色 | 加粗、斜体文本颜色（可设为与正文同色或强调色） |
| 引用块颜色 | 引用标记线颜色、引用文字颜色 |
| 表格颜色 | 表头背景、边框颜色、交替行背景 |

| 操作方式 | 颜色选择器（macOS 原生 ColorPicker）+ 支持 HEX/RGB/HSL 手动输入 |
| 验收 | 颜色选择器弹出后实时预览；支持取色器吸管工具 |

### M03-04 背景设置

| 参数 | 可选值 | 默认 |
|---|---|---|
| 背景类型 | 纯色 / 渐变 / 图片 | 纯色 |
| 渐变方向 | 水平 / 垂直 / 对角 / 径向 | 垂直 |
| 渐变色 | 起始色 + 结束色（颜色选择器） | — |
| 背景图片 | 选择本地图片 / 内置纹理 | — |
| 背景图模式 | 填充 / 适应 / 拉伸 / 居中 | 填充 |
| 背景透明度 | 0%–100% | 100% |
| 背景模糊 | 0px–20px | 0px |

| 验收 | 渐变预览实时显示；背景图片不影响文字可读性时给出提示 |

### M03-05 行高与间距

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| 正文行高 | 1.2–2.5 | 1.7 | 段落内行间距 |
| 代码行高 | 1.2–2.0 | 1.5 | 代码块内行间距 |
| 段落间距 | 0.5–3em | 1em | 段落之间间距 |
| 列表缩进 | 1–4em | 2em | 嵌套列表缩进量 |
| 引用缩进 | 0.5–3em | 1em | 引用块左缩进 |
| 代码块内边距 | 8–32px | 16px | 代码块上下左右内边距 |

### M03-06 字体大小

| 元素 | 范围 | 默认 |
|---|---|---|
| 正文 | 10–24px | 16px |
| H1 | 18–36px | 28px |
| H2 | 16–32px | 24px |
| H3 | 14–28px | 20px |
| H4 | 13–26px | 18px |
| H5 | 12–24px | 16px |
| H6 | 11–22px | 14px |
| 代码 | 10–20px | 14px |
| 小字（脚注/状态） | 10–14px | 12px |

### M03-07 字体颜色

| 元素 | 默认 | 说明 |
|---|---|---|
| 正文 | `#333333` (浅) / `#e0e0e0` (深) | 主文本色 |
| 标题 H1–H6 | 可分别设置 | 默认比正文深 10% |
| 链接 | `#0066cc` (浅) / `#4da3ff` (深) | 超链接 |
| 加粗 | 同正文或自定义 | 强调 |
| 代码 | `#c7254e` (浅) / `#e06c75` (深) | 行内代码 |
| 引用 | `#666666` (浅) / `#999999` (深) | 引用文字 |
| 表头 | 可自定义 | 默认同标题色 |

### M03-08 表格样式

| 参数 | 可选值 | 默认 |
|---|---|---|
| 表格风格 | 简约线框 / 斑马纹 / 边框网格 / 无边框 | 斑马纹 |
| 表头背景色 | 颜色选择器 | 浅灰 |
| 表头字重 | 500/600/700 | 600 |
| 边框颜色 | 颜色选择器 | 浅灰 |
| 边框宽度 | 0–3px | 1px |
| 交替行背景 | 开/关 + 颜色选择器 | 开 |
| 单元格内边距 | 4–20px | 8px |
| 表格宽度 | 自适应内容 / 100% 宽度 | 自适应 |
| 表格对齐 | 左对齐 / 居中 / 右对齐（支持列级 Markdown 语法控制） | 左对齐 |
| 圆角 | 0–12px | 4px |

| 验收 | 表格样式修改后预览即时刷新；交替行颜色与表头背景协调 |

### M03-09 图片设置

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| 最大宽度 | 50%–100% | 100% | 图片最大显示宽度占容器 |
| 默认宽度 | 自适应 / 指定百分比 | 自适应 | 无显式尺寸时的默认 |
| 图片圆角 | 0–20px | 4px | 图片圆角 |
| 图片阴影 | 无 / 浅 / 中 / 深 | 浅 | 投影效果 |
| 图片间距 | 0–2em | 1em | 图片上下间距 |
| 居中对齐 | 开/关 | 开 | 图片默认居中 |
| 点击放大 | 开/关 | 开 | 点击图片全屏查看 |
| 加载占位符 | 文字 / 模糊块 / 无 | 模糊块 | 图片加载中显示 |

### M03-10 主题导入/导出

| 属性 | 说明 |
|---|---|
| 导出格式 | `.fmtheme`（JSON），包含全部主题参数 |
| 导出内容 | 主题名、字体设置、颜色设置、背景设置、表格样式、图片设置、版本号 |
| 导入 | 支持拖拽 `.fmtheme` 文件到主题面板导入 |
| 校验 | 导入时校验 JSON 格式与版本兼容性；不兼容时提示并跳过不支持的参数 |
| 分享 | 导出的主题文件可分享给其他用户直接导入使用 |

---

## M04 Markdown 标准格式支持

### 支持矩阵

基于 CommonMark 0.31 + GFM (GitHub Flavored Markdown) 规范。

| 编号 | 格式元素 | 语法示例 | 渲染效果 | 优先级 |
|---|---|---|---|---|
| F01 | 标题 | `# H1` ~ `###### H6` | 六级标题层级 | P0 |
| F02 | 段落 | 空行分隔 | 段落 | P0 |
| F03 | 加粗 | `**bold**` | **bold** | P0 |
| F04 | 斜体 | `*italic*` | *italic* | P0 |
| F05 | 删除线 | `~~del~~` | ~~del~~ | P0 |
| F06 | 行内代码 | `` `code` `` | `code` | P0 |
| F07 | 代码块 | ` ```lang ... ``` ` | 语法高亮代码块 | P0 |
| F08 | 有序列表 | `1. item` | 编号列表 | P0 |
| F09 | 无序列表 | `- item` | 项目符号列表 | P0 |
| F10 | 任务列表 | `- [x] done` | ☑ 复选框 | P0 |
| F11 | 引用块 | `> quote` | 引用样式块 | P0 |
| F12 | 水平分割线 | `---` | 水平线 | P0 |
| F13 | 链接 | `[text](url)` | 超链接 | P0 |
| F14 | 图片 | `![alt](url)` | 图片渲染 | P0 |

### 各格式渲染规格

#### F01 标题
- H1 下方自动渲染水平分割线（可在设置中关闭）。
- 标题自动生成锚点，支持目录跳转和 URL hash 定位。
- 标题层级嵌套校验（如 H1 直接到 H3 不报错但大纲中显示跳级提示）。

#### F07 代码块
- 支持 200+ 编程语言语法高亮（基于 Shiki）。
- 代码块右上角显示语言标签。
- 支持代码块内行号显示（可开关）。
- 支持代码块折叠（行数超过设定阈值时自动折叠，默认阈值 20 行）。
- 代码块右上角「复制」按钮，一键复制代码内容。
- 支持代码块内高亮指定行（` ```js {1,3-5} ` 语法）。

#### F10 任务列表
- 预览区中复选框可点击切换勾选状态，同步更新源码。
- 编辑区中复选框有可点击的视觉图标。

#### F13 链接
- 支持行内链接、引用式链接、自动链接（裸 URL）。
- 链接 hover 时显示 URL tooltip。
- 外部链接点击时在系统默认浏览器打开；内部链接（`.md` 文件）在编辑器内打开。
- 支持锚点链接跳转（`[章节](#heading)`）。

#### F14 图片
- 支持本地图片（相对路径/绝对路径）和网络图片（URL）。
- 支持图片标题（`![alt](url "title")`），渲染为 caption。
- 支持指定尺寸扩展语法（`![alt](url =200x100)`）。
- 拖拽图片自动复制到 `assets/` 目录并插入相对路径。
- 粘贴图片（截图剪贴板）自动保存为 PNG 并插入。

---

## M05 扩展格式支持

| 编号 | 功能 | 语法 | 渲染效果 | 优先级 |
|---|---|---|---|---|
| E01 | 数学公式（行内） | `$E=mc^2$` | KaTeX 行内渲染 | P0 |
| E02 | 数学公式（块级） | `$$...$$` | KaTeX 居中块级渲染 | P0 |
| E03 | 流程图/图表 | ` ```mermaid ... ``` ` | Mermaid 渲染 | P0 |
| E04 | 目录（TOC） | `[TOC]` 或 `[[toc]]` | 自动生成标题目录 | P0 |
| E05 | 脚注 | `[^1]` + `[^1]: text` | 脚注 + 底部注释 | P0 |
| E06 | 定义列表 | `Term\n: Definition` | 定义列表 | P1 |
| E07 | 表格扩展 | GFM 表格 | 支持列对齐语法 | P0 |
| E08 | Emoji 图标 | `:smile:` | 😀 Emoji 渲染 | P1 |

### E01-E02 数学公式
- 引擎：KaTeX（优先）或 MathJax（可选降级）。
- 支持 LaTeX 语法：分数、根号、求和、积分、矩阵、上下标、希腊字母等。
- 公式渲染失败时以红色错误提示显示原始 LaTeX 源码。
- 支持 `\color{red}{...}` 等颜色语法。
- 支持 `\[...\]` 和 `\(...\)` 语法别名。

### E03 Mermaid 图表
- 支持图表类型：流程图（flowchart）、时序图（sequence）、甘特图（gantt）、类图（class）、状态图（state）、饼图（pie）、ER 图（er）、用户旅程图（journey）。
- 图表渲染失败时显示错误信息和源码。
- 图表支持主题跟随（深色/浅色模式下自动调整图表配色）。
- 图表支持点击放大查看。

### E04 目录（TOC）
- 在文档中插入 `[TOC]` 自动生成当前文档的标题目录树。
- 目录项可点击跳转到对应标题位置。
- 目录自动响应标题变化（增删改后实时更新）。
- 支持设置目录最大层级（如仅显示 H1–H3）。
- 预览区右侧可固定显示「大纲面板」，与 TOC 同步但独立于文档内容。

### E05 脚注
- 支持 `[^id]` 定义脚注引用，`[^id]: 脚注内容` 定义脚注内容。
- 脚注引用渲染为上标数字链接。
- 点击上标跳转到底部脚注区域；脚注区域有返回箭头。
- 脚注编号自动排列。

### E08 Emoji
- 支持 `:shortcode:` 语法（如 `:smile:` → 😀）。
- 内置 Emoji 速查面板（工具栏按钮唤出，可搜索后点击插入）。
- 支持原生 Emoji 字符直接输入。

---

## M06 文件管理

### M06-01 新建文件

| 属性 | 说明 |
|---|---|
| 操作 | `⌘N` / 菜单「文件 → 新建」/ 工具栏按钮 |
| 行为 | 创建空白文档，标题栏显示「未命名」；首次保存时弹出保存对话框 |
| 模板 | 支持选择空白模板、文档模板、技术文档模板、会议纪要模板 |
| 验收 | 新建后光标自动聚焦编辑区 |

### M06-02 打开文件

| 属性 | 说明 |
|---|---|
| 操作 | `⌘O` / 菜单「文件 → 打开」/ 拖拽文件到窗口 |
| 格式 | `.md` `.markdown` `.mdx` `.txt` |
| 编码 | 自动检测 UTF-8；支持手动切换编码（GBK 等） |
| 验收 | 大文件（1MB+）打开 < 2s；打开后光标定位到上次编辑位置（可选） |

### M06-03 保存文件

| 属性 | 说明 |
|---|---|
| 操作 | `⌘S` 保存；`⌘⇧S` 另存为 |
| 自动保存 | 可选开启自动保存（间隔 30s 或失焦时保存） |
| 修改标记 | 未保存时标题栏显示 `●`；保存后消失 |
| 验收 | 保存后文件写入磁盘成功；异常时弹出错误提示 |

### M06-04 打开文件夹

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 打开文件夹」 |
| 行为 | 侧边栏加载文件夹文件树，仅显示 Markdown 文件（可设置显示所有文件） |
| 验收 | 文件夹切换有记忆；最近打开的文件夹列表（最多 10 个） |

### M06-05 文件树操作

| 操作 | 说明 |
|---|---|
| 新建文件 | 右键 → 新建 Markdown 文件，在当前目录创建 |
| 新建文件夹 | 右键 → 新建文件夹 |
| 重命名 | 右键 → 重命名 / 双击文件名；自动更新文档内对该文件的相对引用 |
| 删除 | 右键 → 删除，二次确认；移至废纸篓（macOS 废纸篓可恢复） |
| 在 Finder 中显示 | 右键 → 在 Finder 中显示，定位文件 |
| 移动 | 拖拽文件到目标文件夹 |

### M06-06 最近文件

| 属性 | 说明 |
|---|---|
| 功能 | 菜单「文件 → 最近打开」显示最近 20 个文件，按时间排序 |
| 持久化 | 记录文件路径和最后打开时间 |
| 清理 | 文件不存在时标记为「缺失」，可手动移除 |

### M06-07 自动恢复

| 属性 | 说明 |
|---|---|
| 功能 | 异常退出后重新打开时恢复未保存的编辑内容 |
| 机制 | 每 30s 自动保存草稿到临时目录；正常退出时清理 |
| 验收 | 强制退出后重开，提示「检测到未保存的修改，是否恢复？」 |

---

## M07 导出与分享

### M07-01 导出 PDF

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 导出 → PDF」/ `⌘⇧E` |
| 选项 | 页面大小（A4/Letter）、边距、是否包含目录、页码、页眉页脚 |
| 渲染 | 基于预览区渲染结果导出，保留主题样式 |
| 验收 | 导出的 PDF 与预览效果一致；公式和图表正确渲染 |

### M07-02 导出 HTML

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「文件 → 导出 → HTML」 |
| 选项 | 独立 HTML 文件（内联 CSS/JS）或带资源文件夹 |
| 内容 | 包含渲染后的 HTML + 主题样式 + 必要 JS（Mermaid/KaTeX 可选内联） |
| 验收 | 导出的 HTML 在浏览器中打开效果与编辑器预览一致 |

### M07-03 导出图片

| 属性 | 说明 |
|---|---|
| 操作 | 右键预览区 → 导出为图片 |
| 格式 | PNG / JPEG |
| 选项 | 背景透明（PNG）、分辨率倍数（1x/2x/3x）、裁剪空白边距 |
| 验收 | 导出图片清晰；Retina 屏下 2x/3x 无锯齿 |

### M07-04 复制为 HTML

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「编辑 → 复制为 HTML」/ `⌘⇧C` |
| 行为 | 将预览区渲染后的 HTML（含内联样式）复制到剪贴板 |
| 验收 | 粘贴到富文本编辑器（如邮件、Pages）保留格式 |

### M07-05 复制为纯文本

| 属性 | 说明 |
|---|---|
| 操作 | 菜单「编辑 → 复制为纯文本」/ `⌘⌥C` |
| 行为 | 将 Markdown 去除语法标记后复制为纯文本 |
| 验收 | 粘贴到纯文本编辑器无 Markdown 符号残留 |

### M07-06 打印

| 属性 | 说明 |
|---|---|
| 操作 | `⌘P` / 菜单「文件 → 打印」 |
| 行为 | 调用 macOS 打印对话框，打印预览区渲染结果 |
| 验收 | 打印预览正确分页；公式/图表在打印中可读 |

---

## M08 编辑增强

### M08-01 实时预览

| 属性 | 说明 |
|---|---|
| 行为 | 编辑区输入后 < 100ms 内预览区更新 |
| 增量渲染 | 仅重新渲染变化的部分，大文档不卡顿 |
| 验收 | 输入时无闪烁；滚动同步无明显延迟 |

### M08-02 自动补全

| 触发 | 补全内容 |
|---|---|
| 输入 `` ` `` | 代码语言列表补全（如 `js`, `python`, `go`） |
| 输入 `:` | Emoji 补全（如 `:smile:`） |
| 输入 `[` | 已打开文件夹中的 `.md` 文件名补全（内部链接） |
| 输入 `!` | 图片路径补全 |
| 输入 `#` | 已有标题补全（锚点链接） |

### M08-03 快速格式化

| 操作 | 快捷键 | 行为 |
|---|---|---|
| 加粗 | `⌘B` | 选中文本包裹 `**` |
| 斜体 | `⌘I` | 选中文本包裹 `*` |
| 行内代码 | `⌘E` | 选中文本包裹 `` ` `` |
| 链接 | `⌘K` | 选中文本包裹为 `[text](url)`，光标定位到 `url` |
| 删除线 | `⌘⇧X` | 选中文本包裹 `~~` |
| 任务列表 | `⌘⇧L` | 当前行转为 `- [ ] ` |
| 表格 | `⌘T` | 插入表格模板（行列选择器） |

### M08-04 搜索与替换

| 属性 | 说明 |
|---|---|
| 唤起 | `⌘F` 搜索；`⌘⌥F` 替换 |
| 功能 | 区分大小写、正则表达式、全词匹配 |
| 高亮 | 匹配项高亮；当前匹配项特殊高亮 |
| 统计 | 显示匹配数量和当前位置 |
| 范围 | 当前文件 / 当前文件夹（批量替换） |
| 验收 | 正则替换正确；大文件搜索 < 200ms |

### M08-05 撤销/重做

| 属性 | 说明 |
|---|---|
| 操作 | `⌘Z` 撤销；`⌘⇧Z` 重做 |
| 深度 | 撤销栈深度 500 步 |
| 粒度 | 按词组/操作粒度（非每次按键） |
| 验收 | 跨段落撤销正确恢复光标位置 |

### M08-06 自动保存与草稿恢复

| 属性 | 说明 |
|---|---|
| 策略 | 停止输入 5s 后自动保存；窗口失焦时保存 |
| 草稿 | 未保存的「未命名」文档也保存草稿到临时目录 |
| 验收 | 异常退出后重开恢复全部内容（含光标位置和滚动位置） |

### M08-07 自动格式化

| 属性 | 说明 |
|---|---|
| 触发 | 输入时自动 / 手动 `⌘⇧F` 全文格式化 |
| 规则 | 列表自动续行、缩进对齐、表格列对齐、多余空行压缩 |
| 配置 | 设置中可逐项开关自动格式化规则 |
| 验收 | 格式化不改变渲染结果；仅调整源码排版 |

### M08-08 拼写检查

| 属性 | 说明 |
|---|---|
| 引擎 | macOS 系统拼写检查 |
| 行为 | 编辑区中拼写错误单词下划红色波浪线 |
| 操作 | 右键 → 拼写建议 |
| 验收 | 不误报代码块和行内代码中的内容 |

---

## M09 设置与偏好

### M09-01 通用设置

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 启动时打开 | 上次文件 / 空白文档 / 最近文件列表 | 上次文件 | — |
| 默认编码 | UTF-8 / GBK / 其他 | UTF-8 | — |
| 默认换行符 | LF / CRLF | LF | macOS/Linux 用 LF |
| 检查更新 | 开/关 + 频率 | 开，每日 | 应用更新检查 |
| 语言 | 简体中文 / English | 跟随系统 | 界面语言 |

### M09-02 编辑器设置

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 显示行号 | 开/关 | 开 | 编辑区左侧行号 |
| 自动换行 | 开/关 | 开 | 长行自动换行 |
| Tab 大小 | 2/4/8 | 2 | 缩进宽度 |
| 空格替代 Tab | 开/关 | 开 | — |
| 代码折叠 | 开/关 | 开 | — |
| 显示空白字符 | 开/关 | 关 | 空格/Tab 可视化 |
| 光标样式 | 竖线 / 下划线 / 块 | 竖线 | — |
| 光标闪烁 | 开/关 | 开 | — |
| 编辑区字体大小 | 10–24px | 14px | 源码编辑区字体大小（独立于预览区） |

### M09-03 预览设置

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 预览实时刷新 | 开/关 | 开 | — |
| 预览区字体大小 | 10–24px | 16px | 预览渲染区字体大小 |
| 代码块行号 | 开/关 | 关 | 预览区代码块是否显示行号 |
| 代码块折叠 | 开/关 | 开 | — |
| 图片懒加载 | 开/关 | 开 | 滚动到可视区域才加载图片 |
| 链接在新窗口打开 | 开/关 | 关 | 外部链接打开方式 |

### M09-04 渲染设置

| 设置项 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 数学公式引擎 | KaTeX / MathJax | KaTeX | — |
| Mermaid 主题跟随 | 开/关 | 开 | 图表主题跟随编辑器主题 |
| 脚注渲染 | 开/关 | 开 | — |
| 任务列表可交互 | 开/关 | 开 | 预览区可点击复选框 |
| Emoji 渲染 | 开/关 | 开 | `:shortcode:` → Emoji |
| 自动链接 | 开/关 | 开 | 裸 URL 自动转为链接 |
| 换行渲染 | soft / hard | soft | soft=单换行不渲染为段落；hard=单换行渲染为 `<br>` |

### M09-05 快捷键自定义

| 属性 | 说明 |
|---|---|
| 功能 | 设置面板中可查看和修改所有快捷键 |
| 操作 | 点击快捷键项 → 按下新组合键 → 确认 |
| 冲突检测 | 新设置与系统/应用已有快捷键冲突时提示 |
| 重置 | 每项可单独重置为默认；可一键全部重置 |
| 导入/导出 | 支持快捷键配置导入导出（JSON） |

---

## M10 快捷键体系

### M10-01 全局快捷键

| 快捷键 | 功能 |
|---|---|
| `⌘N` | 新建文件 |
| `⌘O` | 打开文件 |
| `⌘S` | 保存 |
| `⌘⇧S` | 另存为 |
| `⌘W` | 关闭窗口 |
| `⌘Q` | 退出 |
| `⌘P` | 打印 |
| `⌘F` | 搜索 |
| `⌘⌥F` | 替换 |
| `⌘Z` / `⌘⇧Z` | 撤销/重做 |
| `⌘,` | 打开设置 |

### M10-02 视图快捷键

| 快捷键 | 功能 |
|---|---|
| `⌘1` | 仅编辑模式 |
| `⌘2` | 仅预览模式 |
| `⌘3` | 分屏对比模式 |
| `⌘\` | 切换侧边栏 |
| `⌘⇧T` | 切换主题 |
| `⌘+` / `⌘-` | 放大/缩小字体 |
| `⌘0` | 重置字体大小 |
| `⌘⇧F` | 全屏 |

### M10-03 格式化快捷键

| 快捷键 | 功能 |
|---|---|
| `⌘B` | 加粗 |
| `⌘I` | 斜体 |
| `⌘E` | 行内代码 |
| `⌘K` | 插入链接 |
| `⌘T` | 插入表格 |
| `⌘⇧X` | 删除线 |
| `⌘⇧L` | 任务列表 |
| `⌘⇧C` | 复制为 HTML |
| `⌘⌥C` | 复制为纯文本 |
| `⌘⇧E` | 导出 |
| `⌘⇧]` / `⌘⇧[` | 标题层级升降级 |
| `Tab` / `⇧Tab` | 列表缩进/反缩进 |

---

## 5. 非功能需求

### 5.1 性能

| 指标 | 目标 |
|---|---|
| 冷启动时间 | < 1.5s |
| 打开 1MB Markdown 文件 | < 2s |
| 预览渲染延迟 | < 100ms（输入到预览更新） |
| 内存占用 | < 200MB（单文档） |
| 搜索响应 | < 200ms（10万行文档） |
| 主题切换 | < 300ms |

### 5.2 兼容性

| 平台 | 版本 |
|---|---|
| macOS | 13 Ventura 及以上 |
| 架构 | Apple Silicon (arm64) + Intel (x86_64) |

### 5.3 可访问性

- 支持 macOS VoiceOver 屏幕阅读器。
- 所有交互元素支持 Tab 键导航。
- 颜色对比度满足 WCAG 2.1 AA 标准（正文与背景对比度 ≥ 4.5:1）。
- 支持系统「减少动态效果」设置（关闭动画过渡）。
- 支持系统「增加对比度」设置。

### 5.4 安全性

- 不上传用户文档到任何服务器（纯本地应用）。
- 打开外部链接前可设置二次确认。
- 文件写入操作仅在用户明确操作下触发（保存/导出）。
- 不执行 Markdown 中的嵌入式脚本（禁用 `<script>` 标签）。
- HTML 内联渲染白名单：仅允许安全 HTML 标签子集（`<sub>`, `<sup>`, `<kbd>`, `<u>` 等），禁用 `<script>`, `<iframe>`, `<object>`, `<embed>`。

### 5.5 本地化

- 界面语言：简体中文、English（首版），预留多语言框架。
- 日期/时间格式：跟随系统区域设置。
- 输入法：支持 macOS 所有输入法，中文输入法下不触发 Markdown 快捷键。

### 5.6 持久化与配置

| 数据类型 | 存储位置 | 格式 |
|---|---|---|
| 应用设置 | `~/Library/Application Support/FreeMarkdown/` | `settings.json` |
| 自定义主题 | 同上 `/themes/` | `*.fmtheme` (JSON) |
| 快捷键配置 | 同上 | `keybindings.json` |
| 草稿/自动恢复 | 同上 `/drafts/` | 临时 `.md` 文件 |
| 最近文件列表 | 同上 | `recent.json` |

---

## 6. 验收清单

### 6.1 界面与体验

- [ ] macOS 原生窗口体验，红绿灯按钮正常工作
- [ ] 三种布局模式（仅编辑/仅预览/分屏）切换正常，分隔条可拖动
- [ ] 分屏模式下双向滚动同步
- [ ] 侧边栏文件树正常工作（新建/重命名/删除/在 Finder 中显示）
- [ ] 工具栏按钮 hover/active 状态正常
- [ ] 状态栏实时显示字符数/词数/光标位置
- [ ] 拖拽文件/图片/文本正常工作
- [ ] 暗色主题下所有 UI 元素清晰可读

### 6.2 主题

- [ ] 7 个预置主题可正常切换
- [ ] 跟随系统外观模式正常工作
- [ ] 主题切换有过渡动画
- [ ] 主题选择面板有预览卡片
- [ ] 主题导入/导出 `.fmtheme` 正常

### 6.3 自定义主题

- [ ] 字体设置（正文/标题/代码、字号、行高、字重）生效
- [ ] 颜色设置（背景/正文/标题/链接/代码/引用/表格）生效
- [ ] 背景设置（纯色/渐变/图片）生效
- [ ] 表格样式（风格/表头/边框/交替行/圆角）生效
- [ ] 图片设置（最大宽度/圆角/阴影/居中/点击放大）生效
- [ ] 自定义主题可保存/编辑/删除
- [ ] 修改参数后预览 < 100ms 更新

### 6.4 Markdown 格式

- [ ] CommonMark 14 种标准格式全部正确渲染
- [ ] 代码块支持 200+ 语言高亮
- [ ] 代码块复制按钮正常
- [ ] 任务列表可交互（预览区点击切换）
- [ ] 链接内部跳转和外部浏览器打开正常
- [ ] 图片本地/网络/拖拽/粘贴均正常

### 6.5 扩展格式

- [ ] 行内公式 `$...$` 正确渲染
- [ ] 块级公式 `$$...$$` 正确渲染
- [ ] Mermaid 流程图/时序图/甘特图等正确渲染
- [ ] 目录 `[TOC]` 自动生成且可跳转
- [ ] 脚注正确渲染且可跳转
- [ ] 表格列对齐语法正确渲染
- [ ] Emoji `:shortcode:` 正确渲染

### 6.6 文件管理

- [ ] 新建/打开/保存/另存为正常
- [ ] 打开文件夹文件树正常
- [ ] 最近文件列表正确
- [ ] 异常退出后草稿恢复正常

### 6.7 导出

- [ ] 导出 PDF（含目录/页码/页眉页脚）正确
- [ ] 导出 HTML 在浏览器中效果一致
- [ ] 导出图片（PNG/JPEG）清晰
- [ ] 复制为 HTML 保留格式
- [ ] 复制为纯文本无残留符号

### 6.8 编辑增强

- [ ] 实时预览延迟 < 100ms
- [ ] 自动补全（代码语言/Emoji/文件名/标题）正常
- [ ] 快速格式化快捷键正常
- [ ] 搜索替换（正则/大小写/全词）正常
- [ ] 撤销/重做正常
- [ ] 自动保存/草稿恢复正常

### 6.9 设置

- [ ] 所有设置项修改后即时生效
- [ ] 快捷键自定义（修改/冲突检测/重置）正常
- [ ] 设置持久化（重启后保留）

### 6.10 非功能

- [ ] 冷启动 < 1.5s
- [ ] 打开 1MB 文件 < 2s
- [ ] 内存占用 < 200MB
- [ ] VoiceOver 可读编辑区和预览区
- [ ] 颜色对比度满足 WCAG AA

---

## 7. 版本规划

### V1.0（MVP — 本文档范围）

- 完整界面与布局
- 7 个预置主题 + 自定义主题
- CommonMark/GFM 全格式 + 公式/Mermaid/目录/脚注
- 文件管理 + 导出 PDF/HTML
- 编辑增强（实时预览/搜索/补全/格式化）

### V1.1（增量）

- 自动更新机制
- 文档模板库
- 快捷键面板可视化
- 更多预置主题

### V2.0（远期）

- iCloud / WebDAV 同步
- 插件系统
- 多标签页 / 标签管理
- Vim/Emacs 模式
- 协作/分享链接

---

## 8. 变更记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| V1.0 | 2026-09-03 | 初版需求说明书，包含界面/主题/自定义/Mardown格式/扩展格式/文件管理/导出/编辑增强/设置共 10 个功能模块 |
